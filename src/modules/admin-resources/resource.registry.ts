/**
 * THE REGISTRY. One entry per admin list screen; one set of handlers for all of them.
 *
 * ## Adding a resource
 *
 * 1. Add a `defineResource({...})` block below. It needs, at minimum: `slug`,
 *    `module` (which RBAC module gates it — this is what wires up the permission
 *    checks for all seven of its routes), `table`, `primaryKey`, `columns`,
 *    `listColumns`, `fields`, `sortable`, `defaultSort`.
 * 2. List it in `RESOURCES` at the bottom.
 * 3. That is the whole change. `admin-resources.routes.ts` loops over the
 *    registry and calls `defineRoute` seven times per entry, so the endpoints,
 *    the OpenAPI operations, the zod bodies and the permission gates all appear
 *    together. There is no per-resource handler to write.
 *
 * ## What each part controls
 *
 * | Field | Reaches |
 * |---|---|
 * | `columns` | the `?fields=` projection allowlist — nothing outside it is selectable |
 * | `searchable` | the OR-ed `ILIKE` for `?q=` |
 * | `filterable` | `WHERE`; each entry pins its own operators and value type |
 * | `sortable` | `ORDER BY`; unknown fields are a 400, never a silent fallback |
 * | `fields` | the create/update zod bodies AND the console's form spec (§2.6) |
 * | `bulkActions` | `POST /{slug}/bulk`; each declares the extra RBAC action it needs |
 *
 * ## The twelve registered here
 *
 * products, productVariants, collections, designers · customers · coupons,
 * giftCards · banners, faqs, testimonials · suppliers, warehouses.
 *
 * They were chosen to cover every mechanism the engine has: soft delete
 * (`products`) versus status-archive (`banners`), money columns, enums, text
 * arrays, foreign-key references, per-resource page sizes, and all four RBAC
 * modules the generic screens use (`catalogue`, `customers`, `promotions`,
 * `content`, `inventory`). The remaining ~46 slugs in 02_admin_api.md §1 are the
 * same shape.
 */

import {
  addOns,
  addresses,
  approvals,
  banners,
  builderTemplates,
  collections,
  corporateAccounts,
  corporateLeads,
  couriers,
  coupons,
  customerSegments,
  customers,
  deliveryExceptions,
  deliveryZones,
  designers,
  faqs,
  giftCards,
  hamperItems,
  personalisationTemplates,
  productVariants,
  products,
  quotations,
  recipients,
  reviews,
  shipments,
  shippingRules,
  packagingMaterials,
  suppliers,
  testimonials,
  warehouses,
  autoDiscounts,
  upsellRules,
  loyaltyTiers,
  referrals,
  cmsSections, blogPosts, menus, mediaAssets,
  contentPages,
  seoEntries,
  apiKeys, webhooks, integrations, importJobs, activityLogs,
} from '../../db/schema/index.js';
import { enrichCollections } from './enrich.collections.js';
import { enrichCorporateAccounts } from './enrich.corporate-accounts.js';
import { enrichDeliveryZones } from './enrich.delivery-zones.js';
import { enrichHamperItems } from './enrich.hamper-items.js';
import { enrichProductVariants } from './enrich.product-variants.js';
import {
  enrichAutoDiscounts,
  enrichUpsellRules,
  enrichLoyaltyTiers,
  enrichReferrals,
} from './enrich.promotions.js';
import {
  enrichHomepageCms,
  enrichOccasionPages,
  enrichSeoEntries, enrichBlogs, enrichMenus, enrichMediaLibrary,
} from './enrich.content.js';
import { syncDeliveryZonePincodes } from './hooks.delivery-zones.js';
import { eq } from 'drizzle-orm';
import type { FilterSpec, ResourceDescriptor } from './resource.types.js';

/** Identity, so an entry is checked against `ResourceDescriptor` where it is written. */
const defineResource = (descriptor: ResourceDescriptor): ResourceDescriptor => descriptor;

/** The operator sets almost every filter wants. Spelled out so nothing is implicit. */
const EXACT = ['eq', 'ne', 'in'] as const;
const EXACT_NULLABLE = ['eq', 'ne', 'in', 'isNull', 'notNull'] as const;
const RANGE = ['eq', 'ne', 'lt', 'lte', 'gt', 'gte'] as const;
const TEXTUAL = ['eq', 'contains'] as const;

const enumFilter = (
  key: string,
  label: string,
  column: FilterSpec['column'],
  options: readonly string[],
): FilterSpec => ({ key, label, column, valueKind: 'string', operators: [...EXACT], options });

const refFilter = (key: string, label: string, column: FilterSpec['column']): FilterSpec => ({
  key,
  label,
  column,
  valueKind: 'uuid',
  operators: [...EXACT_NULLABLE],
});

const boolFilter = (key: string, label: string, column: FilterSpec['column']): FilterSpec => ({
  key,
  label,
  column,
  valueKind: 'boolean',
  operators: ['eq'],
});

const dateFilter = (key: string, label: string, column: FilterSpec['column']): FilterSpec => ({
  key,
  label,
  column,
  valueKind: 'date',
  operators: [...RANGE],
});

const moneyFilter = (key: string, label: string, column: FilterSpec['column']): FilterSpec => ({
  key,
  label,
  column,
  valueKind: 'number',
  operators: [...RANGE],
});

/* ================================================================ catalogue */

const productsResource = defineResource({
  slug: 'products',
  title: 'Products',
  description: 'The sellable catalogue. `kind` is a fulfilment class, not the storefront category.',
  group: 'Catalogue',
  module: 'catalogue',
  name: { singular: 'Product', plural: 'Products' },
  tag: 'Admin catalogue',
  table: products,
  primaryKey: products.id,
  columns: {
    id: products.id,
    handle: products.handle,
    title: products.title,
    subtitle: products.subtitle,
    description: products.description,
    kind: products.kind,
    designerId: products.designerId,
    primaryCollectionId: products.primaryCollectionId,
    hsnCode: products.hsnCode,
    isPersonalisable: products.isPersonalisable,
    isPerishable: products.isPerishable,
    isFragile: products.isFragile,
    requiresShipping: products.requiresShipping,
    lowStockThreshold: products.lowStockThreshold,
    badgeOverride: products.badgeOverride,
    tags: products.tags,
    status: products.status,
    publishedAt: products.publishedAt,
    createdAt: products.createdAt,
    updatedAt: products.updatedAt,
  },
  listColumns: ['id', 'title', 'handle', 'kind', 'designerId', 'status', 'isPersonalisable', 'publishedAt', 'updatedAt'],
  fields: [
    { key: 'handle', label: 'Handle', kind: 'text', required: true, max: 120, help: 'URL slug. Lower case, hyphenated. Unique among live products.' },
    { key: 'title', label: 'Title', kind: 'text', required: true, max: 200 },
    { key: 'subtitle', label: 'Subtitle', kind: 'text', required: false, max: 200 },
    { key: 'description', label: 'Description', kind: 'long', required: false },
    { key: 'kind', label: 'Fulfilment kind', kind: 'enum', required: true, options: ['hamper', 'single_gift', 'personalised', 'gourmet', 'add_on', 'builder'] },
    { key: 'designerId', label: 'Brand / designer', kind: 'reference', required: false, reference: { resource: 'designers', labelField: 'name' } },
    { key: 'primaryCollectionId', label: 'Primary collection', kind: 'reference', required: false, reference: { resource: 'collections', labelField: 'title' }, help: 'Breadcrumb only — the real taxonomy is the product↔collection join.' },
    { key: 'hsnCode', label: 'HSN code', kind: 'text', required: false, max: 8, help: 'Required before the product can go active — there is no invoice without one.' },
    { key: 'isPersonalisable', label: 'Personalisable', kind: 'boolean', required: false },
    { key: 'isPerishable', label: 'Perishable', kind: 'boolean', required: false },
    { key: 'isFragile', label: 'Fragile', kind: 'boolean', required: false },
    { key: 'requiresShipping', label: 'Requires shipping', kind: 'boolean', required: false },
    { key: 'lowStockThreshold', label: 'Low-stock threshold', kind: 'number', required: false, min: 0, max: 100_000 },
    { key: 'badgeOverride', label: 'Badge override', kind: 'enum', required: false, options: ['best_seller', 'new', 'limited', 'none'] },
    { key: 'tags', label: 'Tags', kind: 'array', required: false, of: { key: 'tag', label: 'Tag', kind: 'text', required: true, max: 40 } },
    { key: 'status', label: 'Status', kind: 'enum', required: true, options: ['active', 'draft', 'archived'] },
    { key: 'publishedAt', label: 'Published at', kind: 'datetime', required: false, help: 'Must be set before status can be `active`.' },
    { key: 'createdAt', label: 'Created', kind: 'datetime', required: false, readOnly: true },
    { key: 'updatedAt', label: 'Updated', kind: 'datetime', required: false, readOnly: true },
  ],
  searchable: ['title', 'handle', 'subtitle'],
  filterable: [
    enumFilter('status', 'Status', products.status, ['active', 'draft', 'archived']),
    enumFilter('kind', 'Kind', products.kind, ['hamper', 'single_gift', 'personalised', 'gourmet', 'add_on', 'builder']),
    refFilter('designerId', 'Brand / designer', products.designerId),
    refFilter('primaryCollectionId', 'Collection', products.primaryCollectionId),
    boolFilter('isPersonalisable', 'Personalisable', products.isPersonalisable),
    dateFilter('publishedAt', 'Published', products.publishedAt),
  ],
  sortable: ['title', 'handle', 'status', 'kind', 'publishedAt', 'createdAt', 'updatedAt'],
  defaultSort: { field: 'updatedAt', direction: 'desc' },
  defaultPerPage: 12,
  softDeleteColumn: products.deletedAt,
  bulkActions: [
    { action: 'publish', label: 'Publish', requires: 'edit', set: { status: 'active' }, description: 'Fails for any product with no HSN code or no publish date — the database enforces both.' },
    { action: 'unpublish', label: 'Move to draft', requires: 'edit', set: { status: 'draft' } },
    { action: 'archive', label: 'Archive', requires: 'delete', set: { status: 'archived' }, destructive: true },
  ],
});

const productVariantsResource = defineResource({
  slug: 'product-variants',
  title: 'Product variants',
  description: 'SKU-level rows. Prices are GST-INCLUSIVE integer paise.',
  group: 'Catalogue',
  module: 'catalogue',
  name: { singular: 'ProductVariant', plural: 'ProductVariants' },
  tag: 'Admin catalogue',
  table: productVariants,
  primaryKey: productVariants.id,
  columns: {
    id: productVariants.id,
    productId: productVariants.productId,
    sku: productVariants.sku,
    optionLabel: productVariants.optionLabel,
    optionValue: productVariants.optionValue,
    pricePaise: productVariants.pricePaise,
    compareAtPaise: productVariants.compareAtPaise,
    costPaise: productVariants.costPaise,
    weightGrams: productVariants.weightGrams,
    lengthMm: productVariants.lengthMm,
    widthMm: productVariants.widthMm,
    heightMm: productVariants.heightMm,
    barcode: productVariants.barcode,
    isDefault: productVariants.isDefault,
    position: productVariants.position,
    status: productVariants.status,
    createdAt: productVariants.createdAt,
    updatedAt: productVariants.updatedAt,
  },
  listColumns: ['id', 'sku', 'productId', 'optionLabel', 'pricePaise', 'weightGrams', 'barcode', 'status', 'updatedAt'],
  // Adds productTitle / productHandle / availableStock / reservedStock. See the
  // module for why these are not `columns`.
  enrich: enrichProductVariants,
  fields: [
    { key: 'productId', label: 'Product', kind: 'reference', required: true, reference: { resource: 'products', labelField: 'title' } },
    { key: 'sku', label: 'SKU', kind: 'text', required: true, max: 64 },
    { key: 'optionLabel', label: 'Option label', kind: 'text', required: true, max: 80, help: '`Signature`, `Rose`, `A5`.' },
    { key: 'optionValue', label: 'Option value', kind: 'text', required: true, max: 80, help: 'Slug form. Unique per product.' },
    { key: 'pricePaise', label: 'Price', kind: 'money', required: true, unit: 'paise', help: 'GST-inclusive.' },
    { key: 'compareAtPaise', label: 'Compare-at price', kind: 'money', required: false, unit: 'paise', help: 'Must be ≥ price. The struck-through number.' },
    { key: 'costPaise', label: 'Cost', kind: 'money', required: false, unit: 'paise', help: 'Never exposed on the storefront API.' },
    { key: 'weightGrams', label: 'Weight', kind: 'number', required: false, unit: 'grams', min: 1, max: 1_000_000 },
    { key: 'lengthMm', label: 'Length', kind: 'number', required: false, unit: 'millimetres', min: 0, max: 100_000 },
    { key: 'widthMm', label: 'Width', kind: 'number', required: false, unit: 'millimetres', min: 0, max: 100_000 },
    { key: 'heightMm', label: 'Height', kind: 'number', required: false, unit: 'millimetres', min: 0, max: 100_000 },
    { key: 'barcode', label: 'Barcode', kind: 'text', required: false, max: 64 },
    { key: 'isDefault', label: 'Default variant', kind: 'boolean', required: false, help: 'At most one per product.' },
    { key: 'position', label: 'Position', kind: 'number', required: false, min: 0, max: 10_000 },
    { key: 'status', label: 'Status', kind: 'enum', required: true, options: ['active', 'inactive'] },
    { key: 'createdAt', label: 'Created', kind: 'datetime', required: false, readOnly: true },
    { key: 'updatedAt', label: 'Updated', kind: 'datetime', required: false, readOnly: true },
  ],
  searchable: ['sku', 'optionLabel', 'barcode'],
  filterable: [
    enumFilter('status', 'Status', productVariants.status, ['active', 'inactive']),
    refFilter('productId', 'Product', productVariants.productId),
    boolFilter('isDefault', 'Default', productVariants.isDefault),
    moneyFilter('pricePaise', 'Price (paise)', productVariants.pricePaise),
  ],
  sortable: ['sku', 'pricePaise', 'position', 'status', 'createdAt', 'updatedAt'],
  defaultSort: { field: 'sku', direction: 'asc' },
  softDeleteColumn: productVariants.deletedAt,
  bulkActions: [
    { action: 'activate', label: 'Activate', requires: 'edit', set: { status: 'active' } },
    { action: 'deactivate', label: 'Deactivate', requires: 'edit', set: { status: 'inactive' }, destructive: true },
  ],
});

const collectionsResource = defineResource({
  slug: 'collections',
  title: 'Collections',
  description: 'One taxonomy table for categories, occasions, festivals, recipients, designer pages and edits.',
  group: 'Catalogue',
  module: 'catalogue',
  name: { singular: 'Collection', plural: 'Collections' },
  tag: 'Admin catalogue',
  table: collections,
  primaryKey: collections.id,
  columns: {
    id: collections.id,
    handle: collections.handle,
    kind: collections.kind,
    parentId: collections.parentId,
    title: collections.title,
    heading: collections.heading,
    subtext: collections.subtext,
    seoDescription: collections.seoDescription,
    designerId: collections.designerId,
    curator: collections.curator,
    sortOrder: collections.sortOrder,
    isFeatured: collections.isFeatured,
    status: collections.status,
    startsOn: collections.startsOn,
    endsOn: collections.endsOn,
    createdAt: collections.createdAt,
    updatedAt: collections.updatedAt,
  },
  listColumns: ['id', 'title', 'handle', 'kind', 'curator', 'status', 'startsOn', 'updatedAt'],
  fields: [
    { key: 'handle', label: 'Handle', kind: 'text', required: true, max: 120, help: 'Public route key. Unique among live collections.' },
    { key: 'kind', label: 'Kind', kind: 'enum', required: true, options: ['category', 'recipient', 'occasion', 'festival', 'designer', 'edit'] },
    { key: 'title', label: 'Nav title', kind: 'text', required: true, max: 160 },
    { key: 'heading', label: 'Page heading', kind: 'text', required: false, max: 200 },
    { key: 'subtext', label: 'Subtext', kind: 'long', required: false },
    { key: 'seoDescription', label: 'SEO description', kind: 'long', required: false, max: 500 },
    { key: 'parentId', label: 'Parent collection', kind: 'reference', required: false, reference: { resource: 'collections', labelField: 'title' } },
    { key: 'designerId', label: 'Designer', kind: 'reference', required: false, reference: { resource: 'designers', labelField: 'name' }, help: 'Only for `kind: designer`.' },
    { key: 'curator', label: 'Curator', kind: 'text', required: false, max: 120, help: 'Only for `kind: edit`.' },
    { key: 'sortOrder', label: 'Sort order', kind: 'number', required: false, min: 0, max: 10_000 },
    { key: 'isFeatured', label: 'Featured', kind: 'boolean', required: false },
    { key: 'status', label: 'Status', kind: 'enum', required: true, options: ['live', 'scheduled', 'draft', 'archived'] },
    { key: 'startsOn', label: 'Starts', kind: 'datetime', required: false, help: 'Required when status is `scheduled`.' },
    { key: 'endsOn', label: 'Ends', kind: 'datetime', required: false },
    { key: 'createdAt', label: 'Created', kind: 'datetime', required: false, readOnly: true },
    { key: 'updatedAt', label: 'Updated', kind: 'datetime', required: false, readOnly: true },
  ],
  searchable: ['title', 'handle', 'curator'],
  filterable: [
    enumFilter('status', 'Status', collections.status, ['live', 'scheduled', 'draft', 'archived']),
    enumFilter('kind', 'Kind', collections.kind, ['category', 'recipient', 'occasion', 'festival', 'designer', 'edit']),
    refFilter('parentId', 'Parent', collections.parentId),
    boolFilter('isFeatured', 'Featured', collections.isFeatured),
  ],
  sortable: ['title', 'handle', 'kind', 'sortOrder', 'status', 'startsOn', 'createdAt', 'updatedAt'],
  defaultSort: { field: 'sortOrder', direction: 'asc' },
  softDeleteColumn: collections.deletedAt,
  bulkActions: [
    { action: 'publish', label: 'Make live', requires: 'edit', set: { status: 'live' } },
    { action: 'feature', label: 'Feature', requires: 'edit', set: { isFeatured: true } },
    { action: 'archive', label: 'Archive', requires: 'delete', set: { status: 'archived' }, destructive: true },
  ],
  enrich: enrichCollections,
});

const designersResource = defineResource({
  slug: 'designers',
  title: 'Brands & designers',
  description: 'Partners. Commission is stored in basis points, not whole percent.',
  group: 'Catalogue',
  module: 'catalogue',
  name: { singular: 'Designer', plural: 'Designers' },
  tag: 'Admin catalogue',
  table: designers,
  primaryKey: designers.id,
  columns: {
    id: designers.id,
    handle: designers.handle,
    name: designers.name,
    kind: designers.kind,
    bio: designers.bio,
    commissionBp: designers.commissionBp,
    contactEmail: designers.contactEmail,
    contactPhone: designers.contactPhone,
    status: designers.status,
    createdAt: designers.createdAt,
    updatedAt: designers.updatedAt,
  },
  listColumns: ['id', 'name', 'handle', 'kind', 'commissionBp', 'contactEmail', 'status', 'updatedAt'],
  fields: [
    { key: 'handle', label: 'Handle', kind: 'text', required: true, max: 120 },
    { key: 'name', label: 'Partner', kind: 'text', required: true, max: 160 },
    { key: 'kind', label: 'Type', kind: 'enum', required: true, options: ['designer', 'brand', 'celebrity', 'artisan_cluster'] },
    { key: 'bio', label: 'Bio', kind: 'long', required: false },
    { key: 'commissionBp', label: 'Commission', kind: 'percent', required: false, unit: 'basis_points', max: 10_000, help: 'The console shows whole percent; the column is basis points. 800 = 8%.' },
    { key: 'contactEmail', label: 'Contact email', kind: 'text', required: false, max: 254 },
    { key: 'contactPhone', label: 'Contact phone', kind: 'text', required: false, max: 20 },
    { key: 'status', label: 'Status', kind: 'enum', required: true, options: ['active', 'paused', 'archived'] },
    { key: 'createdAt', label: 'Created', kind: 'datetime', required: false, readOnly: true },
    { key: 'updatedAt', label: 'Updated', kind: 'datetime', required: false, readOnly: true },
  ],
  searchable: ['name', 'handle', 'contactEmail'],
  filterable: [
    enumFilter('status', 'Status', designers.status, ['active', 'paused', 'archived']),
    enumFilter('kind', 'Type', designers.kind, ['designer', 'brand', 'celebrity', 'artisan_cluster']),
  ],
  sortable: ['name', 'handle', 'kind', 'commissionBp', 'status', 'createdAt', 'updatedAt'],
  defaultSort: { field: 'name', direction: 'asc' },
  softDeleteColumn: designers.deletedAt,
  bulkActions: [
    { action: 'activate', label: 'Activate', requires: 'edit', set: { status: 'active' } },
    { action: 'pause', label: 'Pause', requires: 'edit', set: { status: 'paused' } },
    { action: 'archive', label: 'Archive', requires: 'delete', set: { status: 'archived' }, destructive: true },
  ],
});

/* ================================================================ customers */

const customersResource = defineResource({
  slug: 'customers',
  title: 'Customers',
  description:
    'Shoppers. Lifetime spend and order counts live in the `customer_stats` satellite, refreshed by a ' +
    'job, so they are not writable here.',
  group: 'Customers',
  module: 'customers',
  name: { singular: 'Customer', plural: 'Customers' },
  tag: 'Admin customers',
  table: customers,
  primaryKey: customers.id,
  columns: {
    id: customers.id,
    email: customers.email,
    mobile: customers.mobile,
    fullName: customers.fullName,
    birthday: customers.birthday,
    gender: customers.gender,
    segment: customers.segment,
    corporateAccountId: customers.corporateAccountId,
    defaultBillingGstin: customers.defaultBillingGstin,
    tags: customers.tags,
    marketingOptIn: customers.marketingOptIn,
    whatsappOptIn: customers.whatsappOptIn,
    acceptsCod: customers.acceptsCod,
    blockedAt: customers.blockedAt,
    blockedReason: customers.blockedReason,
    emailVerifiedAt: customers.emailVerifiedAt,
    mobileVerifiedAt: customers.mobileVerifiedAt,
    firstOrderAt: customers.firstOrderAt,
    lastOrderAt: customers.lastOrderAt,
    createdAt: customers.createdAt,
    updatedAt: customers.updatedAt,
  },
  listColumns: ['id', 'fullName', 'email', 'mobile', 'segment', 'tags', 'lastOrderAt', 'createdAt'],
  fields: [
    { key: 'fullName', label: 'Name', kind: 'text', required: false, max: 160 },
    { key: 'email', label: 'Email', kind: 'text', required: false, max: 254, help: 'Case-insensitive. At least one of email or mobile is required.' },
    { key: 'mobile', label: 'Mobile', kind: 'text', required: false, max: 10, help: 'Ten digits starting 6-9.' },
    { key: 'birthday', label: 'Birthday', kind: 'date', required: false },
    { key: 'gender', label: 'Gender', kind: 'enum', required: false, options: ['female', 'male', 'other', 'undisclosed'] },
    { key: 'segment', label: 'Segment', kind: 'enum', required: false, options: ['vip', 'loyal', 'new', 'at_risk', 'corporate_buyer'] },
    { key: 'corporateAccountId', label: 'Corporate account', kind: 'reference', required: false, reference: { resource: 'corporate-accounts', labelField: 'companyName' } },
    { key: 'defaultBillingGstin', label: 'Default billing GSTIN', kind: 'text', required: false, max: 15 },
    { key: 'tags', label: 'Tags', kind: 'array', required: false, of: { key: 'tag', label: 'Tag', kind: 'text', required: true, max: 40 } },
    { key: 'marketingOptIn', label: 'Marketing opt-in', kind: 'boolean', required: false },
    { key: 'whatsappOptIn', label: 'WhatsApp opt-in', kind: 'boolean', required: false },
    { key: 'acceptsCod', label: 'COD allowed', kind: 'boolean', required: false, help: 'Turn off for repeat RTO offenders.' },
    { key: 'blockedReason', label: 'Blocked reason', kind: 'long', required: false },
    { key: 'firstOrderAt', label: 'First order', kind: 'datetime', required: false, readOnly: true },
    { key: 'lastOrderAt', label: 'Last order', kind: 'datetime', required: false, readOnly: true },
    { key: 'createdAt', label: 'Created', kind: 'datetime', required: false, readOnly: true },
    { key: 'updatedAt', label: 'Updated', kind: 'datetime', required: false, readOnly: true },
  ],
  searchable: ['fullName', 'email', 'mobile'],
  filterable: [
    enumFilter('segment', 'Segment', customers.segment, ['vip', 'loyal', 'new', 'at_risk', 'corporate_buyer']),
    refFilter('corporateAccountId', 'Corporate account', customers.corporateAccountId),
    boolFilter('marketingOptIn', 'Marketing opt-in', customers.marketingOptIn),
    boolFilter('acceptsCod', 'COD allowed', customers.acceptsCod),
    { key: 'blockedAt', label: 'Blocked', column: customers.blockedAt, valueKind: 'date', operators: ['isNull', 'notNull', 'lt', 'gt'] },
    dateFilter('lastOrderAt', 'Last order', customers.lastOrderAt),
    dateFilter('createdAt', 'Joined', customers.createdAt),
  ],
  sortable: ['fullName', 'email', 'segment', 'lastOrderAt', 'firstOrderAt', 'createdAt', 'updatedAt'],
  defaultSort: { field: 'createdAt', direction: 'desc' },
  defaultPerPage: 12,
  softDeleteColumn: customers.deletedAt,
  bulkActions: [
    { action: 'mark_vip', label: 'Mark VIP', requires: 'edit', set: { segment: 'vip' } },
    { action: 'block_cod', label: 'Disallow COD', requires: 'edit', set: { acceptsCod: false }, destructive: true },
    { action: 'allow_cod', label: 'Allow COD', requires: 'edit', set: { acceptsCod: true } },
  ],
});

/* =============================================================== promotions */

const couponsResource = defineResource({
  slug: 'coupons',
  title: 'Coupons',
  description:
    'Discount codes. `discountType` decides which value column is required — the database refuses a ' +
    'percent coupon with no basis points, which the console mock could represent and did.',
  group: 'Promotions',
  module: 'promotions',
  name: { singular: 'Coupon', plural: 'Coupons' },
  tag: 'Admin promotions',
  table: coupons,
  primaryKey: coupons.id,
  columns: {
    id: coupons.id,
    code: coupons.code,
    description: coupons.description,
    discountType: coupons.discountType,
    discountBp: coupons.discountBp,
    discountPaise: coupons.discountPaise,
    maxDiscountPaise: coupons.maxDiscountPaise,
    minOrderPaise: coupons.minOrderPaise,
    appliesTo: coupons.appliesTo,
    channels: coupons.channels,
    maxRedemptions: coupons.maxRedemptions,
    maxRedemptionsPerCustomer: coupons.maxRedemptionsPerCustomer,
    redemptionCount: coupons.redemptionCount,
    stackable: coupons.stackable,
    startsAt: coupons.startsAt,
    endsAt: coupons.endsAt,
    status: coupons.status,
    createdAt: coupons.createdAt,
    updatedAt: coupons.updatedAt,
  },
  listColumns: ['id', 'code', 'discountType', 'discountBp', 'discountPaise', 'minOrderPaise', 'redemptionCount', 'maxRedemptions', 'endsAt', 'status'],
  fields: [
    { key: 'code', label: 'Code', kind: 'text', required: true, max: 32, help: 'Upper case, 3-32 characters, `A-Z 0-9 _ -` only.' },
    { key: 'description', label: 'Description', kind: 'long', required: false },
    { key: 'discountType', label: 'Type', kind: 'enum', required: true, options: ['percent', 'flat', 'free_shipping', 'bogo', 'free_gift'] },
    { key: 'discountBp', label: 'Percent off', kind: 'percent', required: false, unit: 'basis_points', max: 10_000, help: 'Required for `percent`.' },
    { key: 'discountPaise', label: 'Flat discount', kind: 'money', required: false, unit: 'paise', help: 'Required for `flat`.' },
    { key: 'maxDiscountPaise', label: 'Cap', kind: 'money', required: false, unit: 'paise', help: 'Caps a percent coupon.' },
    { key: 'minOrderPaise', label: 'Minimum order', kind: 'money', required: false, unit: 'paise' },
    { key: 'appliesTo', label: 'Applies to', kind: 'enum', required: false, options: ['all', 'collections', 'products', 'first_order'] },
    { key: 'channels', label: 'Channels', kind: 'array', required: false, of: { key: 'channel', label: 'Channel', kind: 'text', required: true, max: 32 }, help: 'Empty means every channel.' },
    { key: 'maxRedemptions', label: 'Global limit', kind: 'number', required: false, min: 1, max: 10_000_000 },
    { key: 'maxRedemptionsPerCustomer', label: 'Per-customer limit', kind: 'number', required: false, min: 1, max: 1_000 },
    { key: 'redemptionCount', label: 'Redeemed', kind: 'number', required: false, readOnly: true, help: 'Claimed atomically at order creation. Never write it by hand.' },
    { key: 'stackable', label: 'Stackable', kind: 'boolean', required: false },
    { key: 'startsAt', label: 'Starts', kind: 'datetime', required: false },
    { key: 'endsAt', label: 'Ends', kind: 'datetime', required: false },
    { key: 'status', label: 'Status', kind: 'enum', required: true, options: ['active', 'scheduled', 'expired', 'paused', 'draft'] },
    { key: 'createdAt', label: 'Created', kind: 'datetime', required: false, readOnly: true },
    { key: 'updatedAt', label: 'Updated', kind: 'datetime', required: false, readOnly: true },
  ],
  searchable: ['code', 'description'],
  filterable: [
    enumFilter('status', 'Status', coupons.status, ['active', 'scheduled', 'expired', 'paused', 'draft']),
    enumFilter('discountType', 'Type', coupons.discountType, ['percent', 'flat', 'free_shipping', 'bogo', 'free_gift']),
    enumFilter('appliesTo', 'Applies to', coupons.appliesTo, ['all', 'collections', 'products', 'first_order']),
    boolFilter('stackable', 'Stackable', coupons.stackable),
    dateFilter('endsAt', 'Ends', coupons.endsAt),
    moneyFilter('minOrderPaise', 'Minimum order (paise)', coupons.minOrderPaise),
  ],
  sortable: ['code', 'discountType', 'redemptionCount', 'minOrderPaise', 'startsAt', 'endsAt', 'status', 'createdAt'],
  defaultSort: { field: 'createdAt', direction: 'desc' },
  softDeleteColumn: coupons.deletedAt,
  bulkActions: [
    { action: 'pause', label: 'Pause', requires: 'edit', set: { status: 'paused' } },
    { action: 'activate', label: 'Activate', requires: 'edit', set: { status: 'active' } },
    { action: 'expire', label: 'Expire', requires: 'delete', set: { status: 'expired' }, destructive: true },
  ],
});

const giftCardsResource = defineResource({
  slug: 'gift-cards',
  createUnsupported:
    'Gift cards are issued by a purchase, not created by hand. The card code is generated and hashed at issue time — code_hash and codeLast4 cannot be supplied by a client, so this endpoint could never succeed.',
  title: 'Gift cards',
  description:
    'Issued cards. The code itself is never stored — only its hash and the last four characters — so ' +
    'there is no endpoint, here or anywhere, that can show a customer their code again.',
  group: 'Promotions',
  module: 'promotions',
  name: { singular: 'GiftCard', plural: 'GiftCards' },
  tag: 'Admin promotions',
  table: giftCards,
  primaryKey: giftCards.id,
  columns: {
    id: giftCards.id,
    codeLast4: giftCards.codeLast4,
    initialValuePaise: giftCards.initialValuePaise,
    balancePaise: giftCards.balancePaise,
    currency: giftCards.currency,
    issuedToName: giftCards.issuedToName,
    issuedToEmail: giftCards.issuedToEmail,
    issuedToCustomerId: giftCards.issuedToCustomerId,
    purchaseOrderId: giftCards.purchaseOrderId,
    issuedAt: giftCards.issuedAt,
    expiresOn: giftCards.expiresOn,
    status: giftCards.status,
    createdAt: giftCards.createdAt,
    updatedAt: giftCards.updatedAt,
  },
  listColumns: ['id', 'codeLast4', 'initialValuePaise', 'balancePaise', 'issuedToName', 'issuedAt', 'expiresOn', 'status'],
  fields: [
    { key: 'codeLast4', label: 'Code (last 4)', kind: 'text', required: false, readOnly: true, max: 4, help: 'Display only. The full code exists nowhere on the server.' },
    { key: 'initialValuePaise', label: 'Face value', kind: 'money', required: true, unit: 'paise', min: 1 },
    { key: 'balancePaise', label: 'Balance', kind: 'money', required: false, readOnly: true, unit: 'paise', help: 'Moved only by redemption and top-up ledger rows.' },
    { key: 'issuedToName', label: 'Issued to', kind: 'text', required: false, max: 160 },
    { key: 'issuedToEmail', label: 'Issued to (email)', kind: 'text', required: false, max: 254 },
    { key: 'issuedToCustomerId', label: 'Customer', kind: 'reference', required: false, reference: { resource: 'customers', labelField: 'fullName' } },
    { key: 'expiresOn', label: 'Expires', kind: 'date', required: false },
    { key: 'status', label: 'Status', kind: 'enum', required: true, options: ['active', 'redeemed', 'expired', 'void'] },
    { key: 'issuedAt', label: 'Issued', kind: 'datetime', required: false, readOnly: true },
    { key: 'createdAt', label: 'Created', kind: 'datetime', required: false, readOnly: true },
  ],
  searchable: ['issuedToName', 'issuedToEmail', 'codeLast4'],
  filterable: [
    enumFilter('status', 'Status', giftCards.status, ['active', 'redeemed', 'expired', 'void']),
    refFilter('issuedToCustomerId', 'Customer', giftCards.issuedToCustomerId),
    dateFilter('expiresOn', 'Expiry', giftCards.expiresOn),
    moneyFilter('balancePaise', 'Balance (paise)', giftCards.balancePaise),
  ],
  sortable: ['issuedAt', 'expiresOn', 'balancePaise', 'initialValuePaise', 'status', 'createdAt'],
  defaultSort: { field: 'issuedAt', direction: 'desc' },
  // No `deleted_at` on this table — a gift card is money, so it is voided, never removed.
  archiveStatus: { column: giftCards.status, value: 'void' },
  bulkActions: [
    { action: 'void', label: 'Void', requires: 'delete', set: { status: 'void' }, destructive: true, description: 'Stops the card being redeemable. The balance ledger is untouched.' },
  ],
});

/* ================================================================== content */

const bannersResource = defineResource({
  slug: 'banners',
  title: 'Banners',
  description: 'Storefront banners. Clicks and CTR are analytics and live in `banner_stats_daily`.',
  group: 'Content & Storefront',
  module: 'content',
  name: { singular: 'Banner', plural: 'Banners' },
  tag: 'Admin content',
  table: banners,
  primaryKey: banners.id,
  columns: {
    id: banners.id,
    title: banners.title,
    subtitle: banners.subtitle,
    placement: banners.placement,
    device: banners.device,
    mediaId: banners.mediaId,
    mobileMediaId: banners.mobileMediaId,
    linkUrl: banners.linkUrl,
    collectionId: banners.collectionId,
    ctaLabel: banners.ctaLabel,
    position: banners.position,
    startsAt: banners.startsAt,
    endsAt: banners.endsAt,
    status: banners.status,
    createdAt: banners.createdAt,
    updatedAt: banners.updatedAt,
  },
  listColumns: ['id', 'title', 'placement', 'device', 'startsAt', 'endsAt', 'position', 'status'],
  fields: [
    { key: 'title', label: 'Title', kind: 'text', required: true, max: 200 },
    { key: 'subtitle', label: 'Subtitle', kind: 'text', required: false, max: 300 },
    { key: 'placement', label: 'Placement', kind: 'enum', required: true, options: ['homepage_hero', 'category_top', 'cart_strip', 'pdp_ribbon', 'announcement_bar'] },
    { key: 'device', label: 'Device', kind: 'enum', required: false, options: ['all', 'desktop', 'mobile'] },
    { key: 'mediaId', label: 'Image', kind: 'reference', required: false, reference: { resource: 'media', labelField: 'fileName' } },
    { key: 'mobileMediaId', label: 'Mobile image', kind: 'reference', required: false, reference: { resource: 'media', labelField: 'fileName' } },
    { key: 'linkUrl', label: 'Link URL', kind: 'text', required: false, max: 500 },
    { key: 'collectionId', label: 'Links to collection', kind: 'reference', required: false, reference: { resource: 'collections', labelField: 'title' } },
    { key: 'ctaLabel', label: 'CTA label', kind: 'text', required: false, max: 60 },
    { key: 'position', label: 'Position', kind: 'number', required: false, min: 0, max: 1_000 },
    { key: 'startsAt', label: 'Starts', kind: 'datetime', required: false },
    { key: 'endsAt', label: 'Ends', kind: 'datetime', required: false },
    { key: 'status', label: 'Status', kind: 'enum', required: true, options: ['live', 'scheduled', 'expired', 'draft'] },
    { key: 'createdAt', label: 'Created', kind: 'datetime', required: false, readOnly: true },
    { key: 'updatedAt', label: 'Updated', kind: 'datetime', required: false, readOnly: true },
  ],
  searchable: ['title', 'subtitle', 'ctaLabel'],
  filterable: [
    enumFilter('status', 'Status', banners.status, ['live', 'scheduled', 'expired', 'draft']),
    enumFilter('placement', 'Placement', banners.placement, ['homepage_hero', 'category_top', 'cart_strip', 'pdp_ribbon', 'announcement_bar']),
    enumFilter('device', 'Device', banners.device, ['all', 'desktop', 'mobile']),
    dateFilter('startsAt', 'Starts', banners.startsAt),
    dateFilter('endsAt', 'Ends', banners.endsAt),
  ],
  sortable: ['title', 'placement', 'position', 'startsAt', 'endsAt', 'status', 'createdAt', 'updatedAt'],
  defaultSort: { field: 'position', direction: 'asc' },
  // No `deleted_at` column: a banner is retired by status, and `banner_stats_daily`
  // keeps a foreign key to it either way.
  archiveStatus: { column: banners.status, value: 'expired' },
  bulkActions: [
    { action: 'publish', label: 'Make live', requires: 'edit', set: { status: 'live' } },
    { action: 'expire', label: 'Expire', requires: 'delete', set: { status: 'expired' }, destructive: true },
  ],
});

const faqsResource = defineResource({
  slug: 'faqs',
  title: 'FAQs',
  description: '`answer` is NOT NULL — the console mock had no answer field at all.',
  group: 'Content & Storefront',
  module: 'content',
  name: { singular: 'Faq', plural: 'Faqs' },
  tag: 'Admin content',
  table: faqs,
  primaryKey: faqs.id,
  columns: {
    id: faqs.id,
    question: faqs.question,
    answer: faqs.answer,
    category: faqs.category,
    position: faqs.position,
    helpfulCount: faqs.helpfulCount,
    unhelpfulCount: faqs.unhelpfulCount,
    status: faqs.status,
    createdAt: faqs.createdAt,
    updatedAt: faqs.updatedAt,
  },
  listColumns: ['id', 'question', 'category', 'helpfulCount', 'position', 'status', 'updatedAt'],
  fields: [
    { key: 'question', label: 'Question', kind: 'text', required: true, max: 400 },
    { key: 'answer', label: 'Answer', kind: 'long', required: true },
    { key: 'category', label: 'Category', kind: 'text', required: false, max: 80 },
    { key: 'position', label: 'Position', kind: 'number', required: false, min: 0, max: 1_000 },
    { key: 'helpfulCount', label: 'Marked helpful', kind: 'number', required: false, readOnly: true },
    { key: 'unhelpfulCount', label: 'Marked unhelpful', kind: 'number', required: false, readOnly: true },
    { key: 'status', label: 'Status', kind: 'enum', required: true, options: ['published', 'draft'] },
    { key: 'createdAt', label: 'Created', kind: 'datetime', required: false, readOnly: true },
    { key: 'updatedAt', label: 'Updated', kind: 'datetime', required: false, readOnly: true },
  ],
  searchable: ['question', 'answer', 'category'],
  filterable: [
    enumFilter('status', 'Status', faqs.status, ['published', 'draft']),
    { key: 'category', label: 'Category', column: faqs.category, valueKind: 'string', operators: [...TEXTUAL, 'in', 'isNull'] },
  ],
  sortable: ['question', 'category', 'position', 'helpfulCount', 'status', 'createdAt', 'updatedAt'],
  defaultSort: { field: 'position', direction: 'asc' },
  softDeleteColumn: faqs.deletedAt,
  bulkActions: [
    { action: 'publish', label: 'Publish', requires: 'edit', set: { status: 'published' } },
    { action: 'unpublish', label: 'Move to draft', requires: 'edit', set: { status: 'draft' } },
  ],
});

const testimonialsResource = defineResource({
  slug: 'testimonials',
  title: 'Testimonials',
  description: 'Marketing quotes, not linked to a product. Product reviews are a separate resource.',
  group: 'Content & Storefront',
  module: 'content',
  name: { singular: 'Testimonial', plural: 'Testimonials' },
  tag: 'Admin content',
  table: testimonials,
  primaryKey: testimonials.id,
  columns: {
    id: testimonials.id,
    authorName: testimonials.authorName,
    authorCity: testimonials.authorCity,
    company: testimonials.company,
    designation: testimonials.designation,
    quote: testimonials.quote,
    rating: testimonials.rating,
    mediaId: testimonials.mediaId,
    isFeatured: testimonials.isFeatured,
    position: testimonials.position,
    status: testimonials.status,
    createdAt: testimonials.createdAt,
    updatedAt: testimonials.updatedAt,
  },
  listColumns: ['id', 'authorName', 'company', 'quote', 'rating', 'isFeatured', 'status', 'createdAt'],
  fields: [
    { key: 'authorName', label: 'Author', kind: 'text', required: true, max: 160 },
    { key: 'authorCity', label: 'City', kind: 'text', required: false, max: 80 },
    { key: 'company', label: 'Company', kind: 'text', required: false, max: 160 },
    { key: 'designation', label: 'Designation', kind: 'text', required: false, max: 120 },
    { key: 'quote', label: 'Quote', kind: 'long', required: true, max: 2_000 },
    { key: 'rating', label: 'Rating', kind: 'number', required: false, min: 1, max: 5 },
    { key: 'mediaId', label: 'Photo', kind: 'reference', required: false, reference: { resource: 'media', labelField: 'fileName' } },
    { key: 'isFeatured', label: 'Featured', kind: 'boolean', required: false },
    { key: 'position', label: 'Position', kind: 'number', required: false, min: 0, max: 1_000 },
    { key: 'status', label: 'Status', kind: 'enum', required: true, options: ['published', 'pending', 'rejected'] },
    { key: 'createdAt', label: 'Created', kind: 'datetime', required: false, readOnly: true },
    { key: 'updatedAt', label: 'Updated', kind: 'datetime', required: false, readOnly: true },
  ],
  searchable: ['authorName', 'company', 'quote'],
  filterable: [
    enumFilter('status', 'Status', testimonials.status, ['published', 'pending', 'rejected']),
    boolFilter('isFeatured', 'Featured', testimonials.isFeatured),
    { key: 'rating', label: 'Rating', column: testimonials.rating, valueKind: 'number', operators: [...RANGE, 'isNull'] },
  ],
  sortable: ['authorName', 'company', 'rating', 'position', 'status', 'createdAt', 'updatedAt'],
  defaultSort: { field: 'createdAt', direction: 'desc' },
  archiveStatus: { column: testimonials.status, value: 'rejected' },
  bulkActions: [
    { action: 'publish', label: 'Publish', requires: 'edit', set: { status: 'published' } },
    { action: 'feature', label: 'Feature on homepage', requires: 'edit', set: { isFeatured: true } },
    { action: 'reject', label: 'Reject', requires: 'delete', set: { status: 'rejected' }, destructive: true },
  ],
});

/* ================================================================ inventory */

const suppliersResource = defineResource({
  slug: 'suppliers',
  title: 'Suppliers',
  description: 'Vendors. `outstandingPaise` is a ledger rollup and is not writable here.',
  group: 'Inventory',
  module: 'inventory',
  name: { singular: 'Supplier', plural: 'Suppliers' },
  tag: 'Admin inventory',
  table: suppliers,
  primaryKey: suppliers.id,
  columns: {
    id: suppliers.id,
    code: suppliers.code,
    name: suppliers.name,
    contactName: suppliers.contactName,
    email: suppliers.email,
    mobile: suppliers.mobile,
    line1: suppliers.line1,
    city: suppliers.city,
    stateCode: suppliers.stateCode,
    pincode: suppliers.pincode,
    gstin: suppliers.gstin,
    pan: suppliers.pan,
    category: suppliers.category,
    leadTimeDays: suppliers.leadTimeDays,
    paymentTerms: suppliers.paymentTerms,
    rating: suppliers.rating,
    outstandingPaise: suppliers.outstandingPaise,
    status: suppliers.status,
    createdAt: suppliers.createdAt,
    updatedAt: suppliers.updatedAt,
  },
  listColumns: ['id', 'name', 'contactName', 'mobile', 'city', 'gstin', 'leadTimeDays', 'outstandingPaise', 'status'],
  fields: [
    { key: 'code', label: 'Code', kind: 'text', required: true, max: 32 },
    { key: 'name', label: 'Supplier', kind: 'text', required: true, max: 200 },
    { key: 'contactName', label: 'Contact', kind: 'text', required: false, max: 160 },
    { key: 'email', label: 'Email', kind: 'text', required: false, max: 254 },
    { key: 'mobile', label: 'Mobile', kind: 'text', required: false, max: 20 },
    { key: 'line1', label: 'Address', kind: 'text', required: false, max: 300 },
    { key: 'city', label: 'City', kind: 'text', required: false, max: 120 },
    { key: 'stateCode', label: 'State code', kind: 'text', required: false, max: 2, help: 'Two-digit GST state code.' },
    { key: 'pincode', label: 'PIN code', kind: 'text', required: false, max: 6 },
    { key: 'gstin', label: 'GSTIN', kind: 'text', required: false, max: 15 },
    { key: 'pan', label: 'PAN', kind: 'text', required: false, max: 10 },
    { key: 'category', label: 'Category', kind: 'text', required: false, max: 80, help: 'Gourmet, Packaging, Decor, Fragrance, Logistics.' },
    { key: 'leadTimeDays', label: 'Lead time', kind: 'number', required: false, unit: 'days', min: 0, max: 365 },
    { key: 'paymentTerms', label: 'Payment terms', kind: 'text', required: false, max: 80 },
    { key: 'outstandingPaise', label: 'Outstanding', kind: 'money', required: false, readOnly: true, unit: 'paise', help: 'Rolled up from purchase orders. May be negative.' },
    { key: 'status', label: 'Status', kind: 'enum', required: true, options: ['active', 'on_hold', 'archived'] },
    { key: 'createdAt', label: 'Created', kind: 'datetime', required: false, readOnly: true },
    { key: 'updatedAt', label: 'Updated', kind: 'datetime', required: false, readOnly: true },
  ],
  searchable: ['name', 'contactName', 'city', 'gstin', 'code'],
  filterable: [
    enumFilter('status', 'Status', suppliers.status, ['active', 'on_hold', 'archived']),
    { key: 'category', label: 'Category', column: suppliers.category, valueKind: 'string', operators: [...TEXTUAL, 'in', 'isNull'] },
    { key: 'city', label: 'City', column: suppliers.city, valueKind: 'string', operators: [...TEXTUAL, 'in'] },
    { key: 'leadTimeDays', label: 'Lead time (days)', column: suppliers.leadTimeDays, valueKind: 'number', operators: [...RANGE] },
  ],
  sortable: ['name', 'code', 'city', 'leadTimeDays', 'outstandingPaise', 'status', 'createdAt', 'updatedAt'],
  defaultSort: { field: 'name', direction: 'asc' },
  softDeleteColumn: suppliers.deletedAt,
  bulkActions: [
    { action: 'hold', label: 'Put on hold', requires: 'edit', set: { status: 'on_hold' }, destructive: true },
    { action: 'activate', label: 'Activate', requires: 'edit', set: { status: 'active' } },
    { action: 'archive', label: 'Archive', requires: 'delete', set: { status: 'archived' }, destructive: true },
  ],
});

const warehousesResource = defineResource({
  slug: 'warehouses',
  title: 'Warehouses',
  description:
    'Fulfilment locations. GST registration is state-wise, so each carries its own GSTIN. Filed under ' +
    '“Delivery & Fulfilment” in the nav but gated on `inventory`, matching the console.',
  group: 'Delivery & Fulfilment',
  module: 'inventory',
  name: { singular: 'Warehouse', plural: 'Warehouses' },
  tag: 'Admin inventory',
  table: warehouses,
  primaryKey: warehouses.id,
  columns: {
    id: warehouses.id,
    code: warehouses.code,
    name: warehouses.name,
    line1: warehouses.line1,
    city: warehouses.city,
    stateCode: warehouses.stateCode,
    pincode: warehouses.pincode,
    gstin: warehouses.gstin,
    managerId: warehouses.managerId,
    capacityUnits: warehouses.capacityUnits,
    supportsSameDay: warehouses.supportsSameDay,
    isDefault: warehouses.isDefault,
    status: warehouses.status,
    createdAt: warehouses.createdAt,
    updatedAt: warehouses.updatedAt,
  },
  listColumns: ['id', 'name', 'code', 'city', 'stateCode', 'managerId', 'capacityUnits', 'supportsSameDay', 'status'],
  fields: [
    { key: 'code', label: 'Code', kind: 'text', required: true, max: 32, help: '`WH-MUM-AND`.' },
    { key: 'name', label: 'Warehouse', kind: 'text', required: true, max: 200 },
    { key: 'line1', label: 'Address', kind: 'text', required: true, max: 300 },
    { key: 'city', label: 'City', kind: 'text', required: true, max: 120 },
    { key: 'stateCode', label: 'State code', kind: 'text', required: true, max: 2, help: 'Two-digit GST state code. Determines whether a supply is interstate.' },
    { key: 'pincode', label: 'PIN code', kind: 'text', required: true, max: 6 },
    { key: 'gstin', label: 'GSTIN', kind: 'text', required: false, max: 15, help: 'One per state of operation.' },
    { key: 'managerId', label: 'Manager', kind: 'reference', required: false, reference: { resource: 'team-members', labelField: 'fullName' } },
    { key: 'capacityUnits', label: 'Capacity', kind: 'number', required: false, min: 1, max: 10_000_000 },
    { key: 'supportsSameDay', label: 'Same-day capable', kind: 'boolean', required: false },
    { key: 'isDefault', label: 'Default warehouse', kind: 'boolean', required: false, help: 'Exactly one live warehouse may be the default.' },
    { key: 'status', label: 'Status', kind: 'enum', required: true, options: ['active', 'maintenance', 'closed'] },
    { key: 'createdAt', label: 'Created', kind: 'datetime', required: false, readOnly: true },
    { key: 'updatedAt', label: 'Updated', kind: 'datetime', required: false, readOnly: true },
  ],
  searchable: ['name', 'code', 'city'],
  filterable: [
    enumFilter('status', 'Status', warehouses.status, ['active', 'maintenance', 'closed']),
    { key: 'city', label: 'City', column: warehouses.city, valueKind: 'string', operators: [...TEXTUAL, 'in'] },
    { key: 'stateCode', label: 'State', column: warehouses.stateCode, valueKind: 'string', operators: [...EXACT] },
    boolFilter('supportsSameDay', 'Same-day', warehouses.supportsSameDay),
    boolFilter('isDefault', 'Default', warehouses.isDefault),
  ],
  sortable: ['name', 'code', 'city', 'capacityUnits', 'status', 'createdAt', 'updatedAt'],
  defaultSort: { field: 'name', direction: 'asc' },
  softDeleteColumn: warehouses.deletedAt,
  bulkActions: [
    { action: 'activate', label: 'Activate', requires: 'edit', set: { status: 'active' } },
    { action: 'maintenance', label: 'Mark under maintenance', requires: 'edit', set: { status: 'maintenance' }, destructive: true },
    { action: 'close', label: 'Close', requires: 'delete', set: { status: 'closed' }, destructive: true },
  ],
});

/* ================================================================ the list */

/** Registration order is route order, which is also OpenAPI tag order. */
/**
 * The three catalogue tables that existed with no admin API at all.
 *
 * `hamper_items`, `add_ons` and `personalisation_templates` have been in the
 * schema since the initial migration; the console rendered them from
 * `src/mock/catalog.ts`. Nothing new is created here — no table, no migration,
 * no bespoke handler. Each is a descriptor, which is what this engine exists
 * for, so they inherit pagination, search, filters, sorting, bulk actions,
 * RBAC and audit logging unchanged.
 *
 * The fields below follow the ACTUAL columns rather than the shapes the task
 * brief guessed at: there is no `stockQuantity` on an add-on and no
 * `templateUrl` on a personalisation template, and inventing them would mean
 * inventing storage for them.
 */
const hamperItemsResource = defineResource({
  slug: 'hamper-items',
  title: 'Hamper items',
  description:
    'Individual components a hamper is built from. Costs are integer paise; `cost_paise` is what the ' +
    'item costs US, and is never exposed on the storefront.',
  group: 'Catalogue',
  module: 'catalogue',
  name: { singular: 'HamperItem', plural: 'HamperItems' },
  tag: 'Admin catalogue',
  table: hamperItems,
  primaryKey: hamperItems.id,
  columns: {
    id: hamperItems.id,
    sku: hamperItems.sku,
    name: hamperItems.name,
    supplierId: hamperItems.supplierId,
    category: hamperItems.category,
    costPaise: hamperItems.costPaise,
    unit: hamperItems.unit,
    weightGrams: hamperItems.weightGrams,
    hsnCode: hamperItems.hsnCode,
    isPerishable: hamperItems.isPerishable,
    shelfLifeDays: hamperItems.shelfLifeDays,
    status: hamperItems.status,
    createdAt: hamperItems.createdAt,
    updatedAt: hamperItems.updatedAt,
  },
  listColumns: ['id', 'sku', 'name', 'category', 'costPaise', 'unit', 'isPerishable', 'status', 'updatedAt'],
  fields: [
    { key: 'sku', label: 'SKU', kind: 'text', required: true, max: 64 },
    { key: 'name', label: 'Item', kind: 'text', required: true, max: 160 },
    { key: 'supplierId', label: 'Supplier', kind: 'reference', required: false, reference: { resource: 'suppliers', labelField: 'name' } },
    { key: 'category', label: 'Category', kind: 'text', required: false, max: 80, help: 'Gourmet, Décor, Packaging, Beverage, Wellness.' },
    { key: 'costPaise', label: 'Cost', kind: 'money', required: true, unit: 'paise', help: 'What we pay, not what we charge.' },
    { key: 'unit', label: 'Unit', kind: 'enum', required: true, options: ['pcs', 'box', 'pack', 'kg', 'g', 'ml', 'l'] },
    { key: 'weightGrams', label: 'Weight (g)', kind: 'number', required: false },
    { key: 'hsnCode', label: 'HSN code', kind: 'text', required: false, max: 12, help: 'Must already exist in the HSN table.' },
    { key: 'isPerishable', label: 'Perishable', kind: 'boolean', required: false },
    { key: 'shelfLifeDays', label: 'Shelf life (days)', kind: 'number', required: false },
    { key: 'status', label: 'Status', kind: 'enum', required: true, options: ['active', 'inactive'] },
    { key: 'createdAt', label: 'Created', kind: 'datetime', required: false, readOnly: true },
    { key: 'updatedAt', label: 'Updated', kind: 'datetime', required: false, readOnly: true },
  ],
  searchable: ['name', 'sku', 'category'],
  filterable: [
    enumFilter('status', 'Status', hamperItems.status, ['active', 'inactive']),
    enumFilter('unit', 'Unit', hamperItems.unit, ['pcs', 'box', 'pack', 'kg', 'g', 'ml', 'l']),
    refFilter('supplierId', 'Supplier', hamperItems.supplierId),
    boolFilter('isPerishable', 'Perishable', hamperItems.isPerishable),
    moneyFilter('costPaise', 'Cost (paise)', hamperItems.costPaise),
  ],
  sortable: ['name', 'sku', 'costPaise', 'category', 'status', 'createdAt', 'updatedAt'],
  defaultSort: { field: 'name', direction: 'asc' },
  softDeleteColumn: hamperItems.deletedAt,
  bulkActions: [
    { action: 'activate', label: 'Activate', requires: 'edit', set: { status: 'active' } },
    { action: 'deactivate', label: 'Deactivate', requires: 'edit', set: { status: 'inactive' }, destructive: true },
  ],
  enrich: enrichHamperItems,
});

const addOnsResource = defineResource({
  slug: 'add-ons',
  title: 'Add-ons & packaging',
  description:
    'Wax seals, ribbons, gift boxes, calligraphy notes. `price_paise` is GST-INCLUSIVE, matching every ' +
    'other price in this API.',
  group: 'Catalogue',
  module: 'catalogue',
  name: { singular: 'AddOn', plural: 'AddOns' },
  tag: 'Admin catalogue',
  table: addOns,
  primaryKey: addOns.id,
  columns: {
    id: addOns.id,
    code: addOns.code,
    name: addOns.name,
    kind: addOns.kind,
    pricePaise: addOns.pricePaise,
    hsnCode: addOns.hsnCode,
    requiresInput: addOns.requiresInput,
    inputCharLimit: addOns.inputCharLimit,
    leadTimeHours: addOns.leadTimeHours,
    status: addOns.status,
    createdAt: addOns.createdAt,
    updatedAt: addOns.updatedAt,
  },
  listColumns: ['id', 'code', 'name', 'kind', 'pricePaise', 'requiresInput', 'leadTimeHours', 'status', 'updatedAt'],
  fields: [
    { key: 'code', label: 'Code', kind: 'text', required: true, max: 120, help: 'Lowercase handle form, e.g. `wax-seal-gold`. Unique.' },
    { key: 'name', label: 'Add-on', kind: 'text', required: true, max: 160 },
    { key: 'kind', label: 'Kind', kind: 'enum', required: true, options: ['packaging', 'message', 'fresh', 'bakery', 'digital', 'engraving', 'other'] },
    { key: 'pricePaise', label: 'Price', kind: 'money', required: true, unit: 'paise', help: 'GST-inclusive.' },
    { key: 'hsnCode', label: 'HSN code', kind: 'text', required: false, max: 12 },
    { key: 'requiresInput', label: 'Needs customer input', kind: 'boolean', required: false, help: 'Engraving text, card message.' },
    { key: 'inputCharLimit', label: 'Input character limit', kind: 'number', required: false },
    { key: 'leadTimeHours', label: 'Lead time (hours)', kind: 'number', required: false },
    { key: 'status', label: 'Status', kind: 'enum', required: true, options: ['active', 'inactive'] },
    { key: 'createdAt', label: 'Created', kind: 'datetime', required: false, readOnly: true },
    { key: 'updatedAt', label: 'Updated', kind: 'datetime', required: false, readOnly: true },
  ],
  searchable: ['name', 'code'],
  filterable: [
    enumFilter('status', 'Status', addOns.status, ['active', 'inactive']),
    enumFilter('kind', 'Kind', addOns.kind, ['packaging', 'message', 'fresh', 'bakery', 'digital', 'engraving', 'other']),
    boolFilter('requiresInput', 'Needs input', addOns.requiresInput),
    moneyFilter('pricePaise', 'Price (paise)', addOns.pricePaise),
  ],
  sortable: ['name', 'code', 'kind', 'pricePaise', 'leadTimeHours', 'status', 'createdAt', 'updatedAt'],
  defaultSort: { field: 'name', direction: 'asc' },
  softDeleteColumn: addOns.deletedAt,
  bulkActions: [
    { action: 'activate', label: 'Activate', requires: 'edit', set: { status: 'active' } },
    { action: 'deactivate', label: 'Deactivate', requires: 'edit', set: { status: 'inactive' }, destructive: true },
  ],
});

const personalisationResource = defineResource({
  slug: 'personalisation',
  title: 'Personalisation templates',
  description:
    'Engraving, embroidery, print, digital and laser templates. `surcharge_paise` is what the ' +
    'personalisation adds to the line, in integer paise.',
  group: 'Catalogue',
  module: 'catalogue',
  name: { singular: 'PersonalisationTemplate', plural: 'PersonalisationTemplates' },
  tag: 'Admin catalogue',
  table: personalisationTemplates,
  primaryKey: personalisationTemplates.id,
  columns: {
    id: personalisationTemplates.id,
    name: personalisationTemplates.name,
    method: personalisationTemplates.method,
    turnaroundHours: personalisationTemplates.turnaroundHours,
    charLimit: personalisationTemplates.charLimit,
    allowsImage: personalisationTemplates.allowsImage,
    proofRequired: personalisationTemplates.proofRequired,
    surchargePaise: personalisationTemplates.surchargePaise,
    status: personalisationTemplates.status,
    createdAt: personalisationTemplates.createdAt,
    updatedAt: personalisationTemplates.updatedAt,
  },
  listColumns: ['id', 'name', 'method', 'turnaroundHours', 'charLimit', 'surchargePaise', 'status', 'updatedAt'],
  fields: [
    { key: 'name', label: 'Template', kind: 'text', required: true, max: 160, help: 'Unique among live templates.' },
    { key: 'method', label: 'Method', kind: 'enum', required: true, options: ['engraving', 'embroidery', 'print', 'digital', 'laser'] },
    { key: 'turnaroundHours', label: 'Turnaround (hours)', kind: 'number', required: true, help: 'Must be greater than zero — a CHECK enforces it.' },
    { key: 'charLimit', label: 'Character limit', kind: 'number', required: false, help: 'Null means no limit. Zero is rejected.' },
    { key: 'allowsImage', label: 'Allows image upload', kind: 'boolean', required: false },
    { key: 'proofRequired', label: 'Proof required', kind: 'boolean', required: false },
    { key: 'surchargePaise', label: 'Surcharge', kind: 'money', required: false, unit: 'paise' },
    { key: 'status', label: 'Status', kind: 'enum', required: true, options: ['active', 'draft', 'archived'] },
    { key: 'createdAt', label: 'Created', kind: 'datetime', required: false, readOnly: true },
    { key: 'updatedAt', label: 'Updated', kind: 'datetime', required: false, readOnly: true },
  ],
  searchable: ['name'],
  filterable: [
    enumFilter('status', 'Status', personalisationTemplates.status, ['active', 'draft', 'archived']),
    enumFilter('method', 'Method', personalisationTemplates.method, ['engraving', 'embroidery', 'print', 'digital', 'laser']),
    boolFilter('allowsImage', 'Allows image', personalisationTemplates.allowsImage),
    boolFilter('proofRequired', 'Proof required', personalisationTemplates.proofRequired),
  ],
  sortable: ['name', 'method', 'turnaroundHours', 'surchargePaise', 'status', 'createdAt', 'updatedAt'],
  defaultSort: { field: 'name', direction: 'asc' },
  softDeleteColumn: personalisationTemplates.deletedAt,
  bulkActions: [
    { action: 'activate', label: 'Activate', requires: 'edit', set: { status: 'active' } },
    { action: 'archive', label: 'Archive', requires: 'delete', set: { status: 'archived' }, destructive: true },
  ],
});

/**
 * Build Your Own Hamper templates.
 *
 * The console was falling back to the manufacturing BOM endpoints for this
 * screen, which is the wrong entity: a BOM is an internal assembly recipe, a
 * builder template is what a CUSTOMER is offered. `builder_templates` — with
 * `builder_template_steps` and `builder_step_options` beneath it — has been in
 * the schema all along, so nothing is created here and no relationship to
 * production BOMs is introduced.
 *
 * Slot capacity and allowed categories per slot live in `builder_template_steps`,
 * not on the template, so they are not fields here. Exposing them means a second
 * descriptor for the child rows; this covers the template itself, which is what
 * the screen lists.
 */
const builderTemplatesResource = defineResource({
  slug: 'hamper-builder-templates',
  title: 'Hamper builder templates',
  description:
    'Customer-facing Build Your Own Hamper configurations. NOT manufacturing BOMs — a BOM is an ' +
    'internal assembly recipe. Base price is GST-inclusive integer paise.',
  group: 'Catalogue',
  module: 'catalogue',
  name: { singular: 'BuilderTemplate', plural: 'BuilderTemplates' },
  tag: 'Admin catalogue',
  table: builderTemplates,
  primaryKey: builderTemplates.id,
  columns: {
    id: builderTemplates.id,
    handle: builderTemplates.handle,
    name: builderTemplates.name,
    basePricePaise: builderTemplates.basePricePaise,
    maxWeightGrams: builderTemplates.maxWeightGrams,
    status: builderTemplates.status,
    createdAt: builderTemplates.createdAt,
    updatedAt: builderTemplates.updatedAt,
  },
  listColumns: ['id', 'name', 'handle', 'basePricePaise', 'maxWeightGrams', 'status', 'updatedAt'],
  fields: [
    { key: 'handle', label: 'Handle', kind: 'text', required: true, max: 120, help: 'Lowercase, hyphenated. Unique among live templates.' },
    { key: 'name', label: 'Template', kind: 'text', required: true, max: 160 },
    { key: 'basePricePaise', label: 'Base price', kind: 'money', required: false, unit: 'paise', help: 'Before any item the customer adds.' },
    { key: 'maxWeightGrams', label: 'Max weight (g)', kind: 'number', required: false, help: 'Courier ceiling for the finished hamper. Null means unlimited.' },
    { key: 'status', label: 'Status', kind: 'enum', required: true, options: ['live', 'draft', 'archived'] },
    { key: 'createdAt', label: 'Created', kind: 'datetime', required: false, readOnly: true },
    { key: 'updatedAt', label: 'Updated', kind: 'datetime', required: false, readOnly: true },
  ],
  searchable: ['name', 'handle'],
  filterable: [enumFilter('status', 'Status', builderTemplates.status, ['live', 'draft', 'archived'])],
  sortable: ['name', 'handle', 'basePricePaise', 'status', 'createdAt', 'updatedAt'],
  defaultSort: { field: 'name', direction: 'asc' },
  softDeleteColumn: builderTemplates.deletedAt,
  bulkActions: [
    { action: 'publish', label: 'Publish', requires: 'edit', set: { status: 'live' } },
    { action: 'archive', label: 'Archive', requires: 'delete', set: { status: 'archived' }, destructive: true },
  ],
});

const customerSegmentsResource = defineResource({
  slug: 'segments',
  title: 'Customer segments',
  description: 'Rule-based audiences used for campaigns and personalisation.',
  group: 'Customers',
  module: 'customers',
  name: { singular: 'Segment', plural: 'Segments' },
  tag: 'Admin customers',
  table: customerSegments,
  primaryKey: customerSegments.id,
  columns: {
    id: customerSegments.id,
    name: customerSegments.name,
    rule: customerSegments.rule,
    ruleText: customerSegments.ruleText,
    isDynamic: customerSegments.isDynamic,
    status: customerSegments.status,
    memberCount: customerSegments.memberCount,
    revenuePaise: customerSegments.revenuePaise,
    refreshedAt: customerSegments.refreshedAt,
    createdAt: customerSegments.createdAt,
    updatedAt: customerSegments.updatedAt,
  },
  listColumns: ['id', 'name', 'status', 'memberCount', 'revenuePaise', 'createdAt'],
  fields: [
    { key: 'name', label: 'Name', kind: 'text', required: true, max: 120 },
    { key: 'rule', label: 'Rule', kind: 'text', required: true, help: 'e.g. orders >= 3 AND ltv >= 500000' },
    { key: 'status', label: 'Status', kind: 'enum', required: true, options: ['active', 'draft', 'archived'] },
    { key: 'memberCount', label: 'Customers', kind: 'number', required: false, readOnly: true },
    { key: 'revenuePaise', label: 'Revenue', kind: 'money', required: false, readOnly: true, unit: 'paise' },
    { key: 'createdAt', label: 'Created', kind: 'datetime', required: false, readOnly: true },
    { key: 'updatedAt', label: 'Updated', kind: 'datetime', required: false, readOnly: true },
  ],
  searchable: ['name'],
  filterable: [
    enumFilter('status', 'Status', customerSegments.status, ['active', 'draft', 'archived']),
  ],
  sortable: ['name', 'memberCount', 'revenuePaise', 'createdAt', 'updatedAt'],
  defaultSort: { field: 'createdAt', direction: 'desc' },
  bulkActions: [
    { action: 'activate', label: 'Activate', requires: 'edit', set: { status: 'active' } },
    { action: 'archive', label: 'Archive', requires: 'delete', set: { status: 'archived' }, destructive: true },
  ],
});

const reviewsResource = defineResource({
  slug: 'reviews',
  title: 'Reviews & Testimonials',
  description: 'Moderation queue for product reviews.',
  group: 'Customers',
  module: 'customers',
  name: { singular: 'Review', plural: 'Reviews' },
  tag: 'Admin customers',
  table: reviews,
  primaryKey: reviews.id,
  columns: {
    id: reviews.id,
    productId: reviews.productId,
    customerId: reviews.customerId,
    rating: reviews.rating,
    title: reviews.title,
    body: reviews.body,
    isFeatured: reviews.isFeatured,
    status: reviews.status,
    submittedAt: reviews.submittedAt,
    createdAt: reviews.createdAt,
    updatedAt: reviews.updatedAt,
  },
  listColumns: ['id', 'productId', 'customerId', 'rating', 'title', 'status', 'submittedAt'],
  fields: [
    { key: 'productId', label: 'Product', kind: 'reference', required: true, reference: { resource: 'products', labelField: 'title' } },
    { key: 'customerId', label: 'Customer', kind: 'reference', required: false, reference: { resource: 'customers', labelField: 'fullName' } },
    { key: 'rating', label: 'Rating', kind: 'number', required: true, min: 1, max: 5 },
    { key: 'title', label: 'Title', kind: 'text', required: false },
    { key: 'body', label: 'Body', kind: 'long', required: false },
    { key: 'isFeatured', label: 'Featured', kind: 'boolean', required: false },
    { key: 'status', label: 'Status', kind: 'enum', required: true, options: ['pending', 'published', 'rejected'] },
    { key: 'submittedAt', label: 'Submitted', kind: 'datetime', required: false, readOnly: true },
  ],
  searchable: ['title', 'body'],
  filterable: [
    enumFilter('status', 'Status', reviews.status, ['pending', 'published', 'rejected']),
    refFilter('productId', 'Product', reviews.productId),
    refFilter('customerId', 'Customer', reviews.customerId),
    boolFilter('isFeatured', 'Featured', reviews.isFeatured),
  ],
  sortable: ['rating', 'submittedAt', 'status', 'createdAt', 'updatedAt'],
  defaultSort: { field: 'submittedAt', direction: 'desc' },
  softDeleteColumn: reviews.deletedAt,
  bulkActions: [
    { action: 'publish', label: 'Publish', requires: 'edit', set: { status: 'published' } },
    { action: 'reject', label: 'Reject', requires: 'edit', set: { status: 'rejected' } },
  ],
});

const recipientsResource = defineResource({
  slug: 'recipients',
  title: 'Gift recipients',
  description: 'Recipient book with occasion reminders driving repeat gifting.',
  group: 'Customers',
  module: 'customers',
  name: { singular: 'Recipient', plural: 'Recipients' },
  tag: 'Admin customers',
  table: recipients,
  primaryKey: recipients.id,
  columns: {
    id: recipients.id,
    customerId: recipients.customerId,
    name: recipients.name,
    relation: recipients.relation,
    mobile: recipients.mobile,
    occasion: recipients.occasion,
    nextDate: recipients.nextDate,
    reminderOn: recipients.reminderOn,
    giftsSent: recipients.giftsSent,
    createdAt: recipients.createdAt,
    updatedAt: recipients.updatedAt,
  },
  listColumns: ['id', 'name', 'relation', 'customerId', 'occasion', 'nextDate', 'giftsSent'],
  fields: [
    { key: 'customerId', label: 'Customer', kind: 'reference', required: true, reference: { resource: 'customers', labelField: 'fullName' } },
    { key: 'name', label: 'Name', kind: 'text', required: true, max: 120 },
    { key: 'relation', label: 'Relation', kind: 'text', required: false, max: 80 },
    { key: 'mobile', label: 'Mobile', kind: 'text', required: false, max: 20 },
    { key: 'occasion', label: 'Occasion', kind: 'text', required: false, max: 80 },
    { key: 'nextDate', label: 'Next date', kind: 'date', required: false },
    { key: 'reminderOn', label: 'Reminders active', kind: 'boolean', required: false },
    { key: 'giftsSent', label: 'Gifts sent', kind: 'number', required: false, readOnly: true },
  ],
  searchable: ['name', 'mobile'],
  filterable: [
    refFilter('customerId', 'Customer', recipients.customerId),
    boolFilter('reminderOn', 'Reminders active', recipients.reminderOn),
  ],
  sortable: ['name', 'nextDate', 'giftsSent', 'createdAt'],
  defaultSort: { field: 'createdAt', direction: 'desc' },
  softDeleteColumn: recipients.deletedAt,
  bulkActions: [
    { action: 'enable_reminders', label: 'Enable reminders', requires: 'edit', set: { reminderOn: true } },
    { action: 'disable_reminders', label: 'Disable reminders', requires: 'edit', set: { reminderOn: false } },
  ],
});

const addressesResource = defineResource({
  slug: 'addresses',
  title: 'Saved addresses',
  description: 'Verified delivery addresses across the customer base.',
  group: 'Customers',
  module: 'customers',
  name: { singular: 'Address', plural: 'Addresses' },
  tag: 'Admin customers',
  table: addresses,
  primaryKey: addresses.id,
  columns: {
    id: addresses.id,
    customerId: addresses.customerId,
    label: addresses.label,
    contactName: addresses.contactName,
    mobile: addresses.mobile,
    line1: addresses.line1,
    line2: addresses.line2,
    city: addresses.city,
    stateCode: addresses.stateCode,
    pincode: addresses.pincode,
    isDefault: addresses.isDefault,
    createdAt: addresses.createdAt,
    updatedAt: addresses.updatedAt,
  },
  listColumns: ['id', 'label', 'contactName', 'city', 'stateCode', 'isDefault'],
  fields: [
    { key: 'customerId', label: 'Customer', kind: 'reference', required: true, reference: { resource: 'customers', labelField: 'fullName' } },
    { key: 'label', label: 'Label', kind: 'text', required: true, max: 80 },
    { key: 'contactName', label: 'Contact Name', kind: 'text', required: true, max: 120 },
    { key: 'mobile', label: 'Mobile', kind: 'text', required: true, max: 20 },
    { key: 'line1', label: 'Address Line 1', kind: 'text', required: true, max: 255 },
    { key: 'line2', label: 'Address Line 2', kind: 'text', required: false, max: 255 },
    { key: 'city', label: 'City', kind: 'text', required: true, max: 120 },
    { key: 'stateCode', label: 'State', kind: 'text', required: true, max: 2 },
    { key: 'pincode', label: 'Pincode', kind: 'text', required: true, max: 10 },
    { key: 'isDefault', label: 'Default', kind: 'boolean', required: false },
  ],
  searchable: ['label', 'contactName', 'mobile', 'city', 'pincode'],
  filterable: [
    refFilter('customerId', 'Customer', addresses.customerId),
    boolFilter('isDefault', 'Default', addresses.isDefault),
  ],
  sortable: ['label', 'city', 'createdAt'],
  defaultSort: { field: 'createdAt', direction: 'desc' },
  softDeleteColumn: addresses.deletedAt,
  bulkActions: [],
});

const leadsResource = defineResource({
  slug: 'leads',
  title: 'Corporate Leads',
  description: 'B2B pipeline from enquiry to won, with follow-up scheduling.',
  group: 'Corporate',
  module: 'corporate',
  name: { singular: 'Lead', plural: 'Leads' },
  tag: 'Admin corporate',
  table: corporateLeads,
  primaryKey: corporateLeads.id,
  columns: {
    id: corporateLeads.id,
    companyName: corporateLeads.companyName,
    contactName: corporateLeads.contactName,
    email: corporateLeads.email,
    mobile: corporateLeads.mobile,
    source: corporateLeads.source,
    budgetPaise: corporateLeads.budgetPaise,
    stage: corporateLeads.stage,
    nextFollowUpOn: corporateLeads.nextFollowUpOn,
    createdAt: corporateLeads.createdAt,
    updatedAt: corporateLeads.updatedAt,
  },
  listColumns: ['id', 'companyName', 'contactName', 'stage', 'budgetPaise', 'nextFollowUpOn'],
  fields: [
    { key: 'companyName', label: 'Company', kind: 'text', required: true, max: 120 },
    { key: 'contactName', label: 'Contact Name', kind: 'text', required: true, max: 120 },
    { key: 'email', label: 'Email', kind: 'text', required: true, max: 255 },
    { key: 'mobile', label: 'Mobile', kind: 'text', required: false, max: 20 },
    { key: 'source', label: 'Source', kind: 'text', required: false, max: 120 },
    { key: 'budgetPaise', label: 'Value', kind: 'money', required: false, unit: 'paise' },
    { key: 'stage', label: 'Stage', kind: 'enum', required: true, options: ['new', 'qualified', 'proposal_sent', 'negotiation', 'won', 'lost'] },
    { key: 'nextFollowUpOn', label: 'Next Follow Up', kind: 'date', required: false },
  ],
  searchable: ['companyName', 'contactName', 'email', 'mobile'],
  filterable: [
    enumFilter('stage', 'Stage', corporateLeads.stage, ['new', 'qualified', 'proposal_sent', 'negotiation', 'won', 'lost']),
  ],
  sortable: ['companyName', 'budgetPaise', 'nextFollowUpOn', 'createdAt'],
  defaultSort: { field: 'createdAt', direction: 'desc' },
  softDeleteColumn: corporateLeads.deletedAt,
  bulkActions: [],
});

const corporateAccountsResource = defineResource({
  slug: 'corporate-accounts',
  title: 'Corporate Accounts',
  description: 'Contracted B2B accounts with credit limits and outstanding balances.',
  group: 'Corporate',
  module: 'corporate',
  name: { singular: 'Account', plural: 'Accounts' },
  tag: 'Admin corporate',
  table: corporateAccounts,
  primaryKey: corporateAccounts.id,
  columns: {
    id: corporateAccounts.id,
    companyName: corporateAccounts.companyName,
    gstin: corporateAccounts.gstin,
    accountManagerId: corporateAccounts.accountManagerId,
    creditLimitPaise: corporateAccounts.creditLimitPaise,
    outstandingPaise: corporateAccounts.outstandingPaise,
    paymentTerms: corporateAccounts.paymentTerms,
    status: corporateAccounts.status,
    createdAt: corporateAccounts.createdAt,
    updatedAt: corporateAccounts.updatedAt,
  },
  listColumns: ['id', 'companyName', 'status', 'creditLimitPaise', 'outstandingPaise', 'createdAt'],
  fields: [
    { key: 'companyName', label: 'Company', kind: 'text', required: true, max: 120 },
    { key: 'gstin', label: 'GSTIN', kind: 'text', required: false, max: 15 },
    { key: 'accountManagerId', label: 'SPM', kind: 'reference', required: false, reference: { resource: 'staff', labelField: 'name' } },
    { key: 'creditLimitPaise', label: 'Credit Limit', kind: 'money', required: false, unit: 'paise' },
    { key: 'outstandingPaise', label: 'Outstanding', kind: 'money', required: false, readOnly: true, unit: 'paise' },
    { key: 'paymentTerms', label: 'Terms', kind: 'enum', required: true, options: ['advance', 'net_15', 'net_30', 'net_45', 'net_60'] },
    { key: 'status', label: 'Status', kind: 'enum', required: true, options: ['active', 'credit_hold', 'prospect', 'closed'] },
  ],
  searchable: ['companyName', 'gstin'],
  filterable: [
    enumFilter('status', 'Status', corporateAccounts.status, ['active', 'credit_hold', 'prospect', 'closed']),
    refFilter('accountManagerId', 'Manager', corporateAccounts.accountManagerId),
  ],
  sortable: ['companyName', 'creditLimitPaise', 'outstandingPaise', 'createdAt'],
  defaultSort: { field: 'createdAt', direction: 'desc' },
  softDeleteColumn: corporateAccounts.deletedAt,
  bulkActions: [
    { action: 'suspend', label: 'Put on hold', requires: 'edit', set: { status: 'credit_hold' } },
    { action: 'activate', label: 'Activate', requires: 'edit', set: { status: 'active' } },
  ],
  enrich: enrichCorporateAccounts,
});

const quotationsResource = defineResource({
  slug: 'quotations',
  title: 'Bulk Quotations',
  description: 'Corporate quotes from draft through approval to order conversion.',
  group: 'Corporate',
  module: 'corporate',
  name: { singular: 'Quotation', plural: 'Quotations' },
  tag: 'Admin corporate',
  table: quotations,
  primaryKey: quotations.id,
  columns: {
    id: quotations.id,
    companyName: quotations.companyName,
    subtotalPaise: quotations.subtotalPaise,
    totalPaise: quotations.totalPaise,
    marginBp: quotations.marginBp,
    ownerId: quotations.ownerId,
    validTill: quotations.validTill,
    status: quotations.status,
    createdAt: quotations.createdAt,
    updatedAt: quotations.updatedAt,
  },
  listColumns: ['id', 'companyName', 'status', 'totalPaise', 'marginBp', 'validTill'],
  fields: [
    { key: 'companyName', label: 'Company', kind: 'text', required: true, max: 120 },
    { key: 'totalPaise', label: 'Value', kind: 'money', required: false, readOnly: true, unit: 'paise' },
    { key: 'marginBp', label: 'Margin', kind: 'percent', required: false, readOnly: true, unit: 'basis_points' },
    { key: 'ownerId', label: 'Owner', kind: 'reference', required: false, reference: { resource: 'staff', labelField: 'name' } },
    { key: 'validTill', label: 'Valid Till', kind: 'datetime', required: false },
    { key: 'status', label: 'Status', kind: 'enum', required: true, options: ['draft', 'sent', 'awaiting_approval', 'approved', 'rejected', 'converted', 'expired'] },
  ],
  searchable: ['companyName'],
  filterable: [
    enumFilter('status', 'Status', quotations.status, ['draft', 'sent', 'awaiting_approval', 'approved', 'rejected', 'converted', 'expired']),
    refFilter('ownerId', 'Owner', quotations.ownerId),
  ],
  sortable: ['companyName', 'totalPaise', 'marginBp', 'validTill', 'createdAt'],
  defaultSort: { field: 'createdAt', direction: 'desc' },
  softDeleteColumn: quotations.deletedAt,
  bulkActions: [],
});

const approvalsResource = defineResource({
  slug: 'approvals',
  title: 'Approvals',
  description: 'Discount, credit, refund, and cancellation requests awaiting sign-off.',
  group: 'Corporate',
  module: 'corporate',
  name: { singular: 'Approval', plural: 'Approvals' },
  tag: 'Admin corporate',
  table: approvals,
  primaryKey: approvals.id,
  columns: {
    id: approvals.id,
    kind: approvals.kind,
    subjectLabel: approvals.subjectLabel,
    requestedBy: approvals.requestedBy,
    amountPaise: approvals.amountPaise,
    approverId: approvals.approverId,
    status: approvals.status,
    requestedAt: approvals.requestedAt,
    decidedAt: approvals.decidedAt,
  },
  listColumns: ['id', 'kind', 'subjectLabel', 'requestedBy', 'amountPaise', 'status', 'requestedAt'],
  fields: [
    { key: 'kind', label: 'Type', kind: 'enum', required: true, options: ['quotation_discount', 'credit_limit', 'refund', 'price_change', 'bulk_cancellation'] },
    { key: 'subjectLabel', label: 'Reference', kind: 'text', required: false, max: 120 },
    { key: 'requestedBy', label: 'Requested By', kind: 'reference', required: true, reference: { resource: 'staff', labelField: 'name' } },
    { key: 'amountPaise', label: 'Amount', kind: 'money', required: false, unit: 'paise' },
    { key: 'approverId', label: 'Approver', kind: 'reference', required: false, reference: { resource: 'staff', labelField: 'name' } },
    { key: 'status', label: 'Status', kind: 'enum', required: true, options: ['pending', 'approved', 'rejected', 'withdrawn'] },
  ],
  searchable: ['subjectLabel'],
  filterable: [
    enumFilter('status', 'Status', approvals.status, ['pending', 'approved', 'rejected', 'withdrawn']),
    enumFilter('kind', 'Type', approvals.kind, ['quotation_discount', 'credit_limit', 'refund', 'price_change', 'bulk_cancellation']),
  ],
  sortable: ['requestedAt', 'amountPaise', 'status'],
  defaultSort: { field: 'requestedAt', direction: 'desc' },
  bulkActions: [
    { action: 'approve', label: 'Approve', requires: 'edit', set: { status: 'approved' } },
    { action: 'reject', label: 'Reject', requires: 'edit', set: { status: 'rejected' } },
  ],
});

const deliveryZonesResource = defineResource({
  slug: 'delivery-zones',
  title: 'Delivery Zones',
  description: 'Serviceable pincode clusters with fees, cut-offs and same-day capability.',
  group: 'Delivery',
  module: 'delivery',
  name: { singular: 'Delivery Zone', plural: 'Delivery Zones' },
  tag: 'Admin delivery',
  table: deliveryZones,
  primaryKey: deliveryZones.id,
  columns: {
    id: deliveryZones.id,
    code: deliveryZones.code,
    name: deliveryZones.name,
    city: deliveryZones.city,
    stateCode: deliveryZones.stateCode,
    tier: deliveryZones.tier,
    supportsSameDay: deliveryZones.supportsSameDay,
    supportsMidnight: deliveryZones.supportsMidnight,
    supportsCod: deliveryZones.supportsCod,
    baseFeePaise: deliveryZones.baseFeePaise,
    sameDayCutoff: deliveryZones.sameDayCutoff,
    standardTatDays: deliveryZones.standardTatDays,
    status: deliveryZones.status,
    createdAt: deliveryZones.createdAt,
    updatedAt: deliveryZones.updatedAt,
  },
  listColumns: ['id', 'name', 'stateCode', 'tier', 'baseFeePaise', 'status'],
  fields: [
    { key: 'code', label: 'Code', kind: 'text', required: true, max: 120 },
    { key: 'name', label: 'Name', kind: 'text', required: true, max: 120 },
    { key: 'city', label: 'City', kind: 'text', required: false, max: 120 },
    { key: 'stateCode', label: 'State', kind: 'text', required: false, max: 2 },
    { key: 'tier', label: 'Tier', kind: 'enum', required: false, options: ['metro', 'tier_1', 'tier_2', 'tier_3', 'remote', 'international'] },
    { key: 'baseFeePaise', label: 'Fee', kind: 'money', required: false, unit: 'paise' },
    { key: 'supportsSameDay', label: 'Same Day', kind: 'boolean', required: false },
    { key: 'supportsMidnight', label: 'Midnight', kind: 'boolean', required: false },
    { key: 'supportsCod', label: 'COD', kind: 'boolean', required: false },
    { key: 'sameDayCutoff', label: 'Cut Off', kind: 'text', required: false },
    { key: 'standardTatDays', label: 'TAT Days', kind: 'number', required: false },
    { key: 'status', label: 'Status', kind: 'enum', required: true, options: ['active', 'paused'] },
  ],
  searchable: ['name', 'code', 'city'],
  filterable: [
    enumFilter('status', 'Status', deliveryZones.status, ['active', 'paused']),
    enumFilter('tier', 'Tier', deliveryZones.tier, ['metro', 'tier_1', 'tier_2', 'tier_3', 'remote', 'international']),
  ],
  sortable: ['name', 'baseFeePaise', 'createdAt'],
  defaultSort: { field: 'createdAt', direction: 'desc' },
  bulkActions: [
    { action: 'pause', label: 'Pause', requires: 'edit', set: { status: 'paused' } },
    { action: 'activate', label: 'Activate', requires: 'edit', set: { status: 'active' } },
  ],
  enrich: enrichDeliveryZones,
  afterSave: syncDeliveryZonePincodes,
});

const shippingRulesResource = defineResource({
  slug: 'shipping-rules',
  title: 'Shipping Rules',
  description: 'Rate rules and courier mapping evaluated in priority order.',
  group: 'Delivery',
  module: 'delivery',
  name: { singular: 'Shipping Rule', plural: 'Shipping Rules' },
  tag: 'Admin delivery',
  table: shippingRules,
  primaryKey: shippingRules.id,
  columns: {
    id: shippingRules.id,
    name: shippingRules.name,
    priority: shippingRules.priority,
    conditionText: shippingRules.conditionText,
    chargeKind: shippingRules.chargeKind,
    chargePaise: shippingRules.chargePaise,
    chargeBp: shippingRules.chargeBp,
    preferredCourierId: shippingRules.preferredCourierId,
    stopsEvaluation: shippingRules.stopsEvaluation,
    status: shippingRules.status,
    createdAt: shippingRules.createdAt,
    updatedAt: shippingRules.updatedAt,
  },
  listColumns: ['id', 'priority', 'name', 'conditionText', 'chargeKind', 'status'],
  fields: [
    { key: 'priority', label: 'Priority', kind: 'number', required: true },
    { key: 'name', label: 'Name', kind: 'text', required: true, max: 120 },
    { key: 'conditionText', label: 'Condition', kind: 'text', required: false, max: 255 },
    { key: 'chargeKind', label: 'Charge Kind', kind: 'enum', required: true, options: ['free', 'flat', 'percent', 'per_kg', 'table'] },
    { key: 'chargePaise', label: 'Charge', kind: 'money', required: false, unit: 'paise' },
    { key: 'chargeBp', label: 'Charge %', kind: 'percent', required: false, unit: 'basis_points' },
    { key: 'preferredCourierId', label: 'Courier', kind: 'reference', required: false, reference: { resource: 'couriers', labelField: 'name' } },
    { key: 'stopsEvaluation', label: 'Stops Eval', kind: 'boolean', required: false },
    { key: 'status', label: 'Status', kind: 'enum', required: true, options: ['active', 'inactive'] },
  ],
  searchable: ['name', 'conditionText'],
  filterable: [
    enumFilter('status', 'Status', shippingRules.status, ['active', 'inactive']),
    enumFilter('chargeKind', 'Charge Kind', shippingRules.chargeKind, ['free', 'flat', 'percent', 'per_kg', 'table']),
  ],
  sortable: ['priority', 'name', 'createdAt'],
  defaultSort: { field: 'priority', direction: 'asc' },
  bulkActions: [
    { action: 'deactivate', label: 'Deactivate', requires: 'edit', set: { status: 'inactive' } },
    { action: 'activate', label: 'Activate', requires: 'edit', set: { status: 'active' } },
  ],
});

const couriersResource = defineResource({
  slug: 'couriers',
  title: 'Courier Partners',
  description: 'Partner performance: on-time rate, NDR, RTO and average cost.',
  group: 'Delivery',
  module: 'delivery',
  name: { singular: 'Courier', plural: 'Couriers' },
  tag: 'Admin delivery',
  table: couriers,
  primaryKey: couriers.id,
  columns: {
    id: couriers.id,
    code: couriers.code,
    name: couriers.name,
    supportsCod: couriers.supportsCod,
    supportsInternational: couriers.supportsInternational,
    trackingUrlTemplate: couriers.trackingUrlTemplate,
    baseCostPaise: couriers.baseCostPaise,
    status: couriers.status,
    createdAt: couriers.createdAt,
    updatedAt: couriers.updatedAt,
  },
  listColumns: ['id', 'name', 'code', 'baseCostPaise', 'status', 'createdAt'],
  fields: [
    { key: 'code', label: 'Code', kind: 'text', required: true, max: 120 },
    { key: 'name', label: 'Name', kind: 'text', required: true, max: 120 },
    { key: 'supportsCod', label: 'COD', kind: 'boolean', required: false },
    { key: 'supportsInternational', label: 'International', kind: 'boolean', required: false },
    { key: 'trackingUrlTemplate', label: 'Tracking URL', kind: 'text', required: false, max: 255 },
    { key: 'baseCostPaise', label: 'Cost', kind: 'money', required: false, unit: 'paise' },
    { key: 'status', label: 'Status', kind: 'enum', required: true, options: ['connected', 'disconnected', 'error'] },
  ],
  searchable: ['name', 'code'],
  filterable: [
    enumFilter('status', 'Status', couriers.status, ['connected', 'disconnected', 'error']),
  ],
  sortable: ['name', 'baseCostPaise', 'createdAt'],
  defaultSort: { field: 'createdAt', direction: 'desc' },
  bulkActions: [],
});

const shipmentsResource = defineResource({
  slug: 'shipments',
  title: 'Shipments',
  description: 'Every AWB in flight, with attempts, ETA and live status.',
  group: 'Delivery',
  module: 'delivery',
  name: { singular: 'Shipment', plural: 'Shipments' },
  tag: 'Admin delivery',
  table: shipments,
  primaryKey: shipments.id,
  columns: {
    id: shipments.id,
    shipmentNo: shipments.shipmentNo,
    orderId: shipments.orderId,
    courierId: shipments.courierId,
    awb: shipments.awb,
    weightGrams: shipments.weightGrams,
    shippingCostPaise: shipments.shippingCostPaise,
    isCod: shipments.isCod,
    codAmountPaise: shipments.codAmountPaise,
    status: shipments.status,
    attempts: shipments.attempts,
    etaOn: shipments.etaOn,
    deliveredTo: shipments.deliveredTo,
    createdAt: shipments.createdAt,
    updatedAt: shipments.updatedAt,
  },
  listColumns: ['id', 'shipmentNo', 'awb', 'status', 'etaOn', 'attempts'],
  fields: [
    { key: 'shipmentNo', label: 'Shipment No', kind: 'text', required: true, max: 120 },
    { key: 'orderId', label: 'Order', kind: 'reference', required: true, reference: { resource: 'orders', labelField: 'orderNo' } },
    { key: 'courierId', label: 'Courier', kind: 'reference', required: false, reference: { resource: 'couriers', labelField: 'name' } },
    { key: 'awb', label: 'AWB', kind: 'text', required: false, max: 120 },
    { key: 'weightGrams', label: 'Weight (g)', kind: 'number', required: false },
    { key: 'shippingCostPaise', label: 'Shipping Cost', kind: 'money', required: false, unit: 'paise' },
    { key: 'isCod', label: 'COD', kind: 'boolean', required: false },
    { key: 'codAmountPaise', label: 'COD Amount', kind: 'money', required: false, unit: 'paise' },
    { key: 'status', label: 'Status', kind: 'enum', required: true, options: ['label_created', 'picked_up', 'in_transit', 'out_for_delivery', 'delivered', 'exception', 'rto_initiated', 'rto_delivered', 'cancelled'] },
    { key: 'attempts', label: 'Attempts', kind: 'number', required: false },
    { key: 'etaOn', label: 'ETA', kind: 'date', required: false },
    { key: 'deliveredTo', label: 'Delivered To', kind: 'text', required: false, max: 120 },
  ],
  searchable: ['shipmentNo', 'awb', 'deliveredTo'],
  filterable: [
    enumFilter('status', 'Status', shipments.status, ['label_created', 'picked_up', 'in_transit', 'out_for_delivery', 'delivered', 'exception', 'rto_initiated', 'rto_delivered', 'cancelled']),
    refFilter('courierId', 'Courier', shipments.courierId),
  ],
  sortable: ['shipmentNo', 'etaOn', 'attempts', 'createdAt'],
  defaultSort: { field: 'createdAt', direction: 'desc' },
  bulkActions: [],
});

const exceptionsResource = defineResource({
  slug: 'exceptions',
  title: 'Delivery Exceptions',
  description: 'NDR, RTO, and delay management.',
  group: 'Delivery',
  module: 'delivery',
  name: { singular: 'Exception', plural: 'Exceptions' },
  tag: 'Admin delivery',
  table: deliveryExceptions,
  primaryKey: deliveryExceptions.id,
  columns: {
    id: deliveryExceptions.id,
    exceptionNo: deliveryExceptions.exceptionNo,
    shipmentId: deliveryExceptions.shipmentId,
    orderId: deliveryExceptions.orderId,
    kind: deliveryExceptions.kind,
    reason: deliveryExceptions.reason,
    status: deliveryExceptions.status,
    ownerId: deliveryExceptions.ownerId,
    reattemptOn: deliveryExceptions.reattemptOn,
    createdAt: deliveryExceptions.createdAt,
    updatedAt: deliveryExceptions.updatedAt,
  },
  listColumns: ['id', 'exceptionNo', 'kind', 'reason', 'status', 'reattemptOn'],
  fields: [
    { key: 'exceptionNo', label: 'Exception No', kind: 'text', required: true, max: 120 },
    { key: 'shipmentId', label: 'Shipment', kind: 'reference', required: false, reference: { resource: 'shipments', labelField: 'shipmentNo' } },
    { key: 'orderId', label: 'Order', kind: 'reference', required: true, reference: { resource: 'orders', labelField: 'orderNo' } },
    { key: 'kind', label: 'Type', kind: 'enum', required: true, options: ['ndr', 'rto', 'delay', 'damage', 'address_issue', 'lost'] },
    { key: 'reason', label: 'Reason', kind: 'text', required: true, max: 255 },
    { key: 'status', label: 'Status', kind: 'enum', required: true, options: ['open', 'customer_contacted', 'reattempt_scheduled', 'resolved', 'written_off'] },
    { key: 'ownerId', label: 'Owner', kind: 'reference', required: false, reference: { resource: 'staff', labelField: 'name' } },
    { key: 'reattemptOn', label: 'Reattempt', kind: 'date', required: false },
  ],
  searchable: ['exceptionNo', 'reason'],
  filterable: [
    enumFilter('status', 'Status', deliveryExceptions.status, ['open', 'customer_contacted', 'reattempt_scheduled', 'resolved', 'written_off']),
    enumFilter('kind', 'Type', deliveryExceptions.kind, ['ndr', 'rto', 'delay', 'damage', 'address_issue', 'lost']),
  ],
  sortable: ['exceptionNo', 'reattemptOn', 'createdAt'],
  defaultSort: { field: 'createdAt', direction: 'desc' },
  bulkActions: [
    { action: 'resolve', label: 'Resolve', requires: 'edit', set: { status: 'resolved' } },
  ],
});

const packagingResource = defineResource({
  slug: 'packaging',
  title: 'Packaging & Gift Notes',
  description: 'Packaging materials, dimensions, cost, and note card availability.',
  group: 'Delivery',
  module: 'delivery',
  name: { singular: 'Packaging', plural: 'Packaging Materials' },
  tag: 'Admin delivery',
  table: packagingMaterials,
  primaryKey: packagingMaterials.id,
  columns: {
    id: packagingMaterials.id,
    sku: packagingMaterials.sku,
    name: packagingMaterials.name,
    kind: packagingMaterials.kind,
    lengthMm: packagingMaterials.lengthMm,
    widthMm: packagingMaterials.widthMm,
    heightMm: packagingMaterials.heightMm,
    maxWeightGrams: packagingMaterials.maxWeightGrams,
    costPaise: packagingMaterials.costPaise,
    supportsGiftNote: packagingMaterials.supportsGiftNote,
    supplierId: packagingMaterials.supplierId,
    status: packagingMaterials.status,
    createdAt: packagingMaterials.createdAt,
    updatedAt: packagingMaterials.updatedAt,
  },
  listColumns: ['sku', 'name', 'kind', 'costPaise', 'status'],
  fields: [
    { key: 'sku', label: 'SKU', kind: 'text', required: true, max: 120 },
    { key: 'name', label: 'Name', kind: 'text', required: true, max: 120 },
    { key: 'kind', label: 'Kind', kind: 'enum', required: true, options: ['box', 'trunk', 'potli', 'wrap', 'card', 'filler', 'tape'] },
    { key: 'lengthMm', label: 'Length (mm)', kind: 'number', required: false },
    { key: 'widthMm', label: 'Width (mm)', kind: 'number', required: false },
    { key: 'heightMm', label: 'Height (mm)', kind: 'number', required: false },
    { key: 'maxWeightGrams', label: 'Max Weight (g)', kind: 'number', required: false },
    { key: 'costPaise', label: 'Cost', kind: 'money', required: true, unit: 'paise' },
    { key: 'supportsGiftNote', label: 'Supports Gift Note', kind: 'boolean', required: false },
    { key: 'supplierId', label: 'Supplier', kind: 'reference', required: false, reference: { resource: 'suppliers', labelField: 'name' } },
    { key: 'status', label: 'Status', kind: 'enum', required: true, options: ['active', 'discontinued'] },
  ],
  searchable: ['name', 'sku'],
  filterable: [
    enumFilter('status', 'Status', packagingMaterials.status, ['active', 'discontinued']),
    enumFilter('kind', 'Kind', packagingMaterials.kind, ['box', 'trunk', 'potli', 'wrap', 'card', 'filler', 'tape']),
  ],
  sortable: ['sku', 'name', 'costPaise', 'createdAt'],
  defaultSort: { field: 'createdAt', direction: 'desc' },
  bulkActions: [
    { action: 'discontinue', label: 'Discontinue', requires: 'edit', set: { status: 'discontinued' } },
  ],
  softDeleteColumn: packagingMaterials.deletedAt,
});

const autoDiscountsResource = defineResource({
  slug: 'discounts',
  title: 'Automatic Discounts',
  description: 'Cart-level rules applied without a code.',
  group: 'Promotions',
  module: 'promotions',
  name: { singular: 'Discount', plural: 'Discounts' },
  tag: 'Admin promotions',
  table: autoDiscounts,
  primaryKey: autoDiscounts.id,
  columns: {
    id: autoDiscounts.id,
    name: autoDiscounts.name,
    rule: autoDiscounts.rule,
    ruleText: autoDiscounts.ruleText,
    discountType: autoDiscounts.discountType,
    discountBp: autoDiscounts.discountBp,
    discountPaise: autoDiscounts.discountPaise,
    priority: autoDiscounts.priority,
    stackable: autoDiscounts.stackable,
    status: autoDiscounts.status,
    startsAt: autoDiscounts.startsAt,
    endsAt: autoDiscounts.endsAt,
    createdAt: autoDiscounts.createdAt,
    updatedAt: autoDiscounts.updatedAt,
  },
  listColumns: ['name', 'discountType', 'stackable', 'status', 'startsAt'],
  fields: [
    { key: 'name', label: 'Name', kind: 'text', required: true, max: 120 },
    { key: 'rule', label: 'Rule DSL', kind: 'object', required: true },
    { key: 'discountType', label: 'Discount Type', kind: 'enum', required: true, options: ['percent', 'flat', 'free_shipping', 'free_gift_wrap', 'free_gift'] },
    { key: 'discountBp', label: 'Discount %', kind: 'percent', required: false, unit: 'basis_points' },
    { key: 'discountPaise', label: 'Discount Flat', kind: 'money', required: false, unit: 'paise' },
    { key: 'stackable', label: 'Stackable', kind: 'boolean', required: false },
    { key: 'status', label: 'Status', kind: 'enum', required: true, options: ['active', 'draft', 'expired'] },
  ],
  searchable: ['name'],
  filterable: [
    enumFilter('status', 'Status', autoDiscounts.status, ['active', 'draft', 'expired']),
    enumFilter('discountType', 'Type', autoDiscounts.discountType, ['percent', 'flat', 'free_shipping', 'free_gift_wrap', 'free_gift']),
  ],
  sortable: ['name', 'priority', 'startsAt'],
  defaultSort: { field: 'priority', direction: 'asc' },
  bulkActions: [
    { action: 'activate', label: 'Activate', requires: 'edit', set: { status: 'active' } },
    { action: 'deactivate', label: 'Deactivate', requires: 'edit', set: { status: 'draft' } },
  ],
  enrich: enrichAutoDiscounts,
  softDeleteColumn: autoDiscounts.deletedAt,
});

const upsellRulesResource = defineResource({
  slug: 'upsell',
  title: 'Upsell Rules',
  description: 'Cart, checkout and PDP offers.',
  group: 'Promotions',
  module: 'promotions',
  name: { singular: 'Upsell Rule', plural: 'Upsell Rules' },
  tag: 'Admin promotions',
  table: upsellRules,
  primaryKey: upsellRules.id,
  columns: {
    id: upsellRules.id,
    name: upsellRules.name,
    trigger: upsellRules.trigger,
    triggerText: upsellRules.triggerText,
    offerKind: upsellRules.offerKind,
    offerAddOnId: upsellRules.offerAddOnId,
    offerVariantId: upsellRules.offerVariantId,
    offerPricePaise: upsellRules.offerPricePaise,
    offerDiscountBp: upsellRules.offerDiscountBp,
    placement: upsellRules.placement,
    priority: upsellRules.priority,
    status: upsellRules.status,
    createdAt: upsellRules.createdAt,
    updatedAt: upsellRules.updatedAt,
  },
  listColumns: ['name', 'offerKind', 'placement', 'status'],
  fields: [
    { key: 'name', label: 'Name', kind: 'text', required: true, max: 120 },
    { key: 'trigger', label: 'Trigger DSL', kind: 'object', required: true },
    { key: 'offerKind', label: 'Offer Kind', kind: 'enum', required: true, options: ['add_on', 'product', 'discount', 'free_shipping'] },
    { key: 'offerAddOnId', label: 'Offer Add-On', kind: 'reference', required: false, reference: { resource: 'add-ons', labelField: 'name' } },
    { key: 'offerVariantId', label: 'Offer Product', kind: 'reference', required: false, reference: { resource: 'variants', labelField: 'sku' } },
    { key: 'offerPricePaise', label: 'Fixed Price', kind: 'money', required: false, unit: 'paise' },
    { key: 'offerDiscountBp', label: 'Discount %', kind: 'percent', required: false, unit: 'basis_points' },
    { key: 'placement', label: 'Placement', kind: 'enum', required: true, options: ['pdp', 'cart', 'cart_drawer', 'checkout', 'post_purchase'] },
    { key: 'priority', label: 'Priority', kind: 'number', required: false },
    { key: 'status', label: 'Status', kind: 'enum', required: true, options: ['active', 'paused'] },
  ],
  searchable: ['name'],
  filterable: [
    enumFilter('status', 'Status', upsellRules.status, ['active', 'paused']),
    enumFilter('placement', 'Placement', upsellRules.placement, ['pdp', 'cart', 'cart_drawer', 'checkout', 'post_purchase']),
  ],
  sortable: ['name', 'priority', 'createdAt'],
  defaultSort: { field: 'priority', direction: 'asc' },
  bulkActions: [
    { action: 'activate', label: 'Activate', requires: 'edit', set: { status: 'active' } },
    { action: 'pause', label: 'Pause', requires: 'edit', set: { status: 'paused' } },
  ],
  enrich: enrichUpsellRules,
});

const loyaltyTiersResource = defineResource({
  slug: 'loyalty',
  title: 'Loyalty Tiers',
  description: 'Tier thresholds, earn rates and perks.',
  group: 'Promotions',
  module: 'promotions',
  name: { singular: 'Tier', plural: 'Loyalty Tiers' },
  tag: 'Admin promotions',
  table: loyaltyTiers,
  primaryKey: loyaltyTiers.id,
  columns: {
    id: loyaltyTiers.id,
    name: loyaltyTiers.name,
    rank: loyaltyTiers.rank,
    thresholdPaise: loyaltyTiers.thresholdPaise,
    pointsPer100Paise: loyaltyTiers.pointsPer100Paise,
    perks: loyaltyTiers.perks,
    isInviteOnly: loyaltyTiers.isInviteOnly,
    freeSameDay: loyaltyTiers.freeSameDay,
    freeGiftWrap: loyaltyTiers.freeGiftWrap,
    discountBp: loyaltyTiers.discountBp,
    status: loyaltyTiers.status,
    createdAt: loyaltyTiers.createdAt,
    updatedAt: loyaltyTiers.updatedAt,
  },
  listColumns: ['name', 'rank', 'thresholdPaise', 'status'],
  fields: [
    { key: 'name', label: 'Tier Name', kind: 'text', required: true, max: 120 },
    { key: 'rank', label: 'Rank', kind: 'number', required: true },
    { key: 'thresholdPaise', label: 'Spend Threshold', kind: 'money', required: true, unit: 'paise' },
    { key: 'pointsPer100Paise', label: 'Points / ₹100', kind: 'number', required: true },
    { key: 'perks', label: 'Perks', kind: 'text', required: false, max: 255 },
    { key: 'isInviteOnly', label: 'Invite Only', kind: 'boolean', required: false },
    { key: 'freeSameDay', label: 'Free Same Day Delivery', kind: 'boolean', required: false },
    { key: 'freeGiftWrap', label: 'Free Gift Wrap', kind: 'boolean', required: false },
    { key: 'discountBp', label: 'Tier Discount', kind: 'percent', required: false, unit: 'basis_points' },
    { key: 'status', label: 'Status', kind: 'enum', required: true, options: ['active', 'archived'] },
  ],
  searchable: ['name'],
  filterable: [
    enumFilter('status', 'Status', loyaltyTiers.status, ['active', 'archived']),
  ],
  sortable: ['rank', 'name', 'thresholdPaise'],
  defaultSort: { field: 'rank', direction: 'asc' },
  bulkActions: [
    { action: 'archive', label: 'Archive', requires: 'delete', set: { status: 'archived' }, destructive: true },
  ],
  enrich: enrichLoyaltyTiers,
});

const referralsResource = defineResource({
  slug: 'referrals',
  title: 'Referral Programme',
  description: 'Advocate performance and rewards.',
  group: 'Promotions',
  module: 'promotions',
  name: { singular: 'Referral Code', plural: 'Referral Codes' },
  tag: 'Admin promotions',
  table: referrals,
  primaryKey: referrals.id,
  columns: {
    id: referrals.id,
    referrerCustomerId: referrals.referrerCustomerId,
    code: referrals.code,
    rewardKind: referrals.rewardKind,
    rewardValue: referrals.rewardValue,
    status: referrals.status,
    createdAt: referrals.createdAt,
    updatedAt: referrals.updatedAt,
  },
  listColumns: ['code', 'rewardKind', 'rewardValue', 'status'],
  fields: [
    { key: 'code', label: 'Code', kind: 'text', required: true, max: 120 },
    { key: 'referrerCustomerId', label: 'Advocate', kind: 'reference', required: true, reference: { resource: 'customers', labelField: 'email' } },
    { key: 'rewardKind', label: 'Reward Kind', kind: 'enum', required: true, options: ['points', 'coupon', 'store_credit'] },
    { key: 'rewardValue', label: 'Reward Value', kind: 'number', required: true },
    { key: 'status', label: 'Status', kind: 'enum', required: true, options: ['active', 'blocked'] },
  ],
  searchable: ['code'],
  filterable: [
    enumFilter('status', 'Status', referrals.status, ['active', 'blocked']),
    enumFilter('rewardKind', 'Type', referrals.rewardKind, ['points', 'coupon', 'store_credit']),
  ],
  sortable: ['code', 'createdAt'],
  defaultSort: { field: 'createdAt', direction: 'desc' },
  bulkActions: [
    { action: 'block', label: 'Block', requires: 'edit', set: { status: 'blocked' } },
    { action: 'activate', label: 'Activate', requires: 'edit', set: { status: 'active' } },
  ],
  enrich: enrichReferrals,
});

const homepageCmsResource = defineResource({
  slug: 'homepage-cms',
  title: 'Homepage CMS',
  description: 'Storefront sections, ordering, and visibility control.',
  group: 'Content & Storefront',
  module: 'content',
  name: { singular: 'Section', plural: 'Homepage Sections' },
  tag: 'Admin content',
  table: cmsSections,
  primaryKey: cmsSections.id,
  columns: {
    id: cmsSections.id,
    key: cmsSections.key,
    pageKey: cmsSections.pageKey,
    title: cmsSections.title,
    layout: cmsSections.layout,
    position: cmsSections.position,
    isVisible: cmsSections.isVisible,
    settings: cmsSections.settings,
    createdAt: cmsSections.createdAt,
    updatedAt: cmsSections.updatedAt,
  },
  listColumns: ['title', 'layout', 'position', 'isVisible'],
  fields: [
    { key: 'key', label: 'Section Key', kind: 'text', required: true, max: 120 },
    { key: 'title', label: 'Section Title', kind: 'text', required: true, max: 120 },
    { key: 'layout', label: 'Layout', kind: 'enum', required: true, options: ['full_bleed', 'grid_4', 'grid_3', 'carousel', 'split_banner', 'marquee', 'list'] },
    { key: 'position', label: 'Position', kind: 'number', required: true },
    { key: 'isVisible', label: 'Visible', kind: 'boolean', required: false },
    { key: 'settings', label: 'Settings JSON', kind: 'object', required: false },
  ],
  searchable: ['title', 'key'],
  filterable: [
    enumFilter('isVisible', 'Visible', cmsSections.isVisible, ['true', 'false']),
    enumFilter('layout', 'Layout', cmsSections.layout, ['full_bleed', 'grid_4', 'grid_3', 'carousel', 'split_banner', 'marquee', 'list']),
  ],
  sortable: ['position', 'title'],
  defaultSort: { field: 'position', direction: 'asc' },
  bulkActions: [
    { action: 'show', label: 'Show', requires: 'edit', set: { isVisible: true } },
    { action: 'hide', label: 'Hide', requires: 'edit', set: { isVisible: false } },
  ],
  enrich: enrichHomepageCms,
});

const occasionPagesResource = defineResource({
  slug: 'occasions',
  title: 'Occasion Pages',
  description: 'Landing pages by occasion with traffic and conversion metrics.',
  group: 'Content & Storefront',
  module: 'content',
  name: { singular: 'Occasion Page', plural: 'Occasion Pages' },
  tag: 'Admin content',
  table: contentPages,
  primaryKey: contentPages.id,
  baseFilter: eq(contentPages.kind, 'occasion'),
  baseSet: { kind: 'occasion' },
  columns: {
    id: contentPages.id,
    slug: contentPages.slug,
    kind: contentPages.kind,
    title: contentPages.title,
    heading: contentPages.heading,
    bodyBlocks: contentPages.bodyBlocks,
    collectionId: contentPages.collectionId,
    heroMediaId: contentPages.heroMediaId,
    status: contentPages.status,
    publishedAt: contentPages.publishedAt,
    createdAt: contentPages.createdAt,
    updatedAt: contentPages.updatedAt,
  },
  listColumns: ['title', 'slug', 'status'],
  fields: [
    { key: 'title', label: 'Page Title', kind: 'text', required: true, max: 120 },
    { key: 'slug', label: 'URL Slug', kind: 'text', required: true, max: 120 },
    { key: 'heading', label: 'Heading', kind: 'text', required: false, max: 120 },
    { key: 'bodyBlocks', label: 'Body Blocks', kind: 'array', required: false },
    { key: 'collectionId', label: 'Linked Collection', kind: 'reference', required: false, reference: { resource: 'collections', labelField: 'name' } },
    { key: 'heroMediaId', label: 'Hero Media', kind: 'reference', required: false, reference: { resource: 'media-library', labelField: 'filename' } },
    { key: 'status', label: 'Status', kind: 'enum', required: true, options: ['published', 'draft', 'archived'] },
  ],
  searchable: ['title', 'slug', 'heading'],
  filterable: [
    enumFilter('status', 'Status', contentPages.status, ['published', 'draft', 'archived']),
  ],
  sortable: ['title', 'slug', 'createdAt'],
  defaultSort: { field: 'createdAt', direction: 'desc' },
  bulkActions: [
    { action: 'publish', label: 'Publish', requires: 'edit', set: { status: 'published' } },
    { action: 'archive', label: 'Archive', requires: 'delete', set: { status: 'archived' }, destructive: true },
  ],
  enrich: enrichOccasionPages,
  softDeleteColumn: contentPages.deletedAt,
});

const seoEntriesResource = defineResource({
  slug: 'seo',
  title: 'SEO Manager',
  description: 'Meta titles, keyword positions, and index status.',
  group: 'Content & Storefront',
  module: 'content',
  name: { singular: 'SEO Entry', plural: 'SEO Entries' },
  tag: 'Admin content',
  table: seoEntries,
  primaryKey: seoEntries.id,
  baseFilter: eq(seoEntries.entityType, 'route'),
  baseSet: { entityType: 'route' },
  columns: {
    id: seoEntries.id,
    entityType: seoEntries.entityType,
    entityId: seoEntries.entityId,
    routePath: seoEntries.routePath,
    metaTitle: seoEntries.metaTitle,
    metaDescription: seoEntries.metaDescription,
    canonicalUrl: seoEntries.canonicalUrl,
    ogMediaId: seoEntries.ogMediaId,
    focusKeyword: seoEntries.focusKeyword,
    robotsIndex: seoEntries.robotsIndex,
    robotsFollow: seoEntries.robotsFollow,
    structuredData: seoEntries.structuredData,
    createdAt: seoEntries.createdAt,
    updatedAt: seoEntries.updatedAt,
  },
  listColumns: ['routePath', 'metaTitle', 'focusKeyword', 'robotsIndex'],
  fields: [
    { key: 'routePath', label: 'Route Path', kind: 'text', required: true, max: 255 },
    { key: 'metaTitle', label: 'Meta Title', kind: 'text', required: false, max: 60 },
    { key: 'metaDescription', label: 'Meta Description', kind: 'text', required: false, max: 160 },
    { key: 'canonicalUrl', label: 'Canonical URL', kind: 'text', required: false, max: 255 },
    { key: 'focusKeyword', label: 'Focus Keyword', kind: 'text', required: false, max: 60 },
    { key: 'robotsIndex', label: 'Indexable', kind: 'boolean', required: false },
    { key: 'robotsFollow', label: 'Follow Links', kind: 'boolean', required: false },
    { key: 'structuredData', label: 'Structured Data', kind: 'object', required: false },
  ],
  searchable: ['routePath', 'metaTitle', 'focusKeyword'],
  filterable: [
    enumFilter('robotsIndex', 'Indexed', seoEntries.robotsIndex, ['true', 'false']),
  ],
  sortable: ['routePath', 'metaTitle', 'focusKeyword'],
  defaultSort: { field: 'routePath', direction: 'asc' },
  bulkActions: [
    { action: 'index', label: 'Index', requires: 'edit', set: { robotsIndex: true } },
    { action: 'noindex', label: 'No Index', requires: 'edit', set: { robotsIndex: false } },
  ],
  enrich: enrichSeoEntries,
});


const blogPostsResource = defineResource({
  slug: 'blogs',
  title: 'Blogs',
  description: 'Editorial journal posts, authors, and readership.',
  group: 'Content & Storefront',
  module: 'content',
  name: { singular: 'Blog Post', plural: 'Blog Posts' },
  tag: 'Admin content',
  table: blogPosts,
  primaryKey: blogPosts.id,
  columns: {
    id: blogPosts.id,
    slug: blogPosts.slug,
    title: blogPosts.title,
    excerpt: blogPosts.excerpt,
    bodyBlocks: blogPosts.bodyBlocks,
    category: blogPosts.category,
    status: blogPosts.status,
    publishedAt: blogPosts.publishedAt,
    createdAt: blogPosts.createdAt,
    updatedAt: blogPosts.updatedAt,
  },
  listColumns: ['title', 'category', 'status', 'publishedAt'],
  fields: [
    { key: 'title', label: 'Title', kind: 'text', required: true, max: 255 },
    { key: 'slug', label: 'Slug', kind: 'text', required: true, max: 255 },
    { key: 'category', label: 'Category', kind: 'text', required: false, max: 120 },
    { key: 'excerpt', label: 'Excerpt', kind: 'text', required: false },
    { key: 'status', label: 'Status', kind: 'enum', required: true, options: ['published', 'draft', 'scheduled', 'archived'] },
  ],
  searchable: ['title', 'slug', 'category'],
  filterable: [
    enumFilter('status', 'Status', blogPosts.status, ['published', 'draft', 'scheduled', 'archived']),
  ],
  sortable: ['title', 'publishedAt', 'createdAt'],
  defaultSort: { field: 'publishedAt', direction: 'desc' },
  bulkActions: [
    { action: 'publish', label: 'Publish', requires: 'edit', set: { status: 'published' } },
    { action: 'archive', label: 'Archive', requires: 'delete', set: { status: 'archived' }, destructive: true },
  ],
  enrich: enrichBlogs,
});

const menusResource = defineResource({
  slug: 'menus',
  title: 'Navigation Menus',
  description: 'Header, footer, and mobile navigation structure.',
  group: 'Content & Storefront',
  module: 'content',
  name: { singular: 'Menu', plural: 'Menus' },
  tag: 'Admin content',
  table: menus,
  primaryKey: menus.id,
  columns: {
    id: menus.id,
    key: menus.key,
    name: menus.name,
    createdAt: menus.createdAt,
    updatedAt: menus.updatedAt,
  },
  listColumns: ['name', 'key'],
  fields: [
    { key: 'name', label: 'Menu Name', kind: 'text', required: true, max: 120 },
    { key: 'key', label: 'Identifier Key', kind: 'text', required: true, max: 120 },
  ],
  searchable: ['name', 'key'],
  filterable: [],
  sortable: ['name', 'key', 'createdAt'],
  defaultSort: { field: 'name', direction: 'asc' },
  bulkActions: [],
  enrich: enrichMenus,
});

const integrationsResource = defineResource({
  slug: 'integrations',
  title: 'Integrations',
  description: 'Third-party integrations and credentials.',
  group: 'Settings',
  module: 'settings',
  name: { singular: 'Integration', plural: 'Integrations' },
  tag: 'Admin Settings',
  table: integrations,
  primaryKey: integrations.id,
  columns: {
    id: integrations.id,
    key: integrations.key,
    name: integrations.name,
    category: integrations.category,
    status: integrations.status,
    credentialsRef: integrations.credentialsRef,
    config: integrations.config,
    createdAt: integrations.createdAt,
    updatedAt: integrations.updatedAt,
  },
  listColumns: ['name', 'category', 'status', 'createdAt'],
  fields: [
    { key: 'key', kind: 'text', required: true, label: 'Key' },
    { key: 'name', kind: 'text', required: true, label: 'Name' },
    { key: 'category', kind: 'text', required: true, label: 'Category' },
    { key: 'status', kind: 'text', required: true, label: 'Status' },
    { key: 'credentialsRef', kind: 'text', required: false, label: 'Credentials' },
    { key: 'config', kind: 'object', required: false, label: 'Config' },
  ],
  filterable: [enumFilter('status', 'Status', integrations.status, ['connected', 'not_connected', 'error'])],
  searchable: ['name', 'key'],
  sortable: ['createdAt'],
  defaultSort: { field: 'createdAt', direction: 'desc' },
  bulkActions: [],
});

const webhooksResource = defineResource({
  slug: 'webhooks',
  title: 'Webhooks',
  description: 'Event-driven outgoing webhooks.',
  group: 'Settings',
  module: 'settings',
  name: { singular: 'Webhook', plural: 'Webhooks' },
  tag: 'Admin Settings',
  table: webhooks,
  primaryKey: webhooks.id,
  columns: {
    id: webhooks.id,
    endpointUrl: webhooks.endpointUrl,
    description: webhooks.description,
    events: webhooks.events,
    status: webhooks.status,
    consecutiveFailures: webhooks.consecutiveFailures,
    lastDeliveryAt: webhooks.lastDeliveryAt,
    createdAt: webhooks.createdAt,
    updatedAt: webhooks.updatedAt,
  },
  listColumns: ['endpointUrl', 'events', 'status', 'lastDeliveryAt'],
  fields: [
    { key: 'endpointUrl', kind: 'text', required: true, label: 'Endpoint URL' },
    { key: 'description', kind: 'text', required: false, label: 'Description' },
    { key: 'events', kind: 'array', required: true, label: 'Events', of: { key: '', label: '', kind: 'text', required: true } },
    { key: 'status', kind: 'text', required: false, label: 'Status' },
  ],
  filterable: [enumFilter('status', 'Status', webhooks.status, ['healthy', 'failing', 'paused', 'disabled'])],
  searchable: ['endpointUrl'],
  sortable: ['createdAt'],
  defaultSort: { field: 'createdAt', direction: 'desc' },
  bulkActions: [],
});

const apiKeysResource = defineResource({
  slug: 'api-keys',
  title: 'API Keys',
  description: 'Programmatic access keys.',
  group: 'Settings',
  module: 'settings',
  name: { singular: 'API Key', plural: 'API Keys' },
  tag: 'Admin Settings',
  table: apiKeys,
  primaryKey: apiKeys.id,
  columns: {
    id: apiKeys.id,
    label: apiKeys.label,
    keyPrefix: apiKeys.keyPrefix,
    environment: apiKeys.environment,
    scopes: apiKeys.scopes,
    lastUsedAt: apiKeys.lastUsedAt,
    createdAt: apiKeys.createdAt,
  },
  listColumns: ['label', 'keyPrefix', 'environment', 'createdAt'],
  fields: [
    { key: 'label', kind: 'text', required: true, label: 'Label' },
    { key: 'environment', kind: 'text', required: false, label: 'Environment' },
    { key: 'scopes', kind: 'array', required: true, label: 'Scopes', of: { key: '', label: '', kind: 'text', required: true } },
  ],
  filterable: [enumFilter('environment', 'Environment', apiKeys.environment, ['live', 'sandbox'])],
  searchable: ['label'],
  sortable: ['createdAt'],
  defaultSort: { field: 'createdAt', direction: 'desc' },
  bulkActions: [],
});

const auditLogsResource = defineResource({
  slug: 'activity-logs',
  title: 'Activity Logs',
  description: 'Activity and audit logs.',
  group: 'Settings',
  module: 'settings',
  name: { singular: 'Audit Log', plural: 'Audit Logs' },
  tag: 'Admin Settings',
  table: activityLogs,
  primaryKey: activityLogs.id,
  columns: {
    id: activityLogs.id,
    entityType: activityLogs.entityType,
    entityId: activityLogs.entityId,
    action: activityLogs.action,
    actorStaffId: activityLogs.actorStaffId,
    actorCustomerId: activityLogs.actorCustomerId,
    actorApiKeyId: activityLogs.actorApiKeyId,
    occurredAt: activityLogs.occurredAt,
  },
  listColumns: ['entityType', 'action', 'occurredAt'],
  fields: [],
  filterable: [],
  searchable: ['entityType', 'action'],
  sortable: ['occurredAt'],
  defaultSort: { field: 'occurredAt', direction: 'desc' },
  bulkActions: [],
});

const importJobsResource = defineResource({
  slug: 'import-jobs',
  title: 'Import & Export Jobs',
  description: 'CSV bulk operations.',
  group: 'Settings',
  module: 'settings',
  name: { singular: 'Import Job', plural: 'Import Jobs' },
  tag: 'Admin Settings',
  table: importJobs,
  primaryKey: importJobs.id,
  columns: {
    id: importJobs.id,
    entity: importJobs.entity,
    mode: importJobs.mode,
    status: importJobs.status,
    totalRows: importJobs.totalRows,
    succeededRows: importJobs.succeededRows,
    failedRows: importJobs.failedRows,
    startedAt: importJobs.startedAt,
    finishedAt: importJobs.finishedAt,
    createdAt: importJobs.createdAt,
  },
  listColumns: ['entity', 'mode', 'status', 'totalRows', 'createdAt'],
  fields: [
    { key: 'entity', kind: 'text', required: true, label: 'Entity' },
    { key: 'mode', kind: 'text', required: true, label: 'Mode' },
    { key: 'status', kind: 'text', required: false, label: 'Status' },
  ],
  filterable: [
    enumFilter('status', 'Status', importJobs.status, ['queued', 'processing', 'completed', 'failed', 'cancelled']),
    enumFilter('entity', 'Entity', importJobs.entity, ['products', 'variants', 'inventory', 'customers', 'campaign_recipients', 'pincodes', 'collections']),
  ],
  searchable: [],
  sortable: ['createdAt'],
  defaultSort: { field: 'createdAt', direction: 'desc' },
  bulkActions: [],
});

const mediaLibraryResource = defineResource({
  slug: 'media',
  title: 'Media Library',
  description: 'Product photography, banners, and video assets.',
  group: 'Content & Storefront',
  module: 'content',
  name: { singular: 'Media Asset', plural: 'Media Assets' },
  tag: 'Admin content',
  table: mediaAssets,
  primaryKey: mediaAssets.id,
  columns: {
    id: mediaAssets.id,
    filename: mediaAssets.filename,
    mimeType: mediaAssets.mimeType,
    bytes: mediaAssets.bytes,
    widthPx: mediaAssets.widthPx,
    heightPx: mediaAssets.heightPx,
    url: mediaAssets.url,
    altText: mediaAssets.altText,
    createdAt: mediaAssets.createdAt,
    updatedAt: mediaAssets.updatedAt,
  },
  listColumns: ['filename', 'mimeType', 'bytes'],
  fields: [
    { key: 'filename', label: 'Filename', kind: 'text', required: true, max: 255 },
    { key: 'mimeType', label: 'MIME Type', kind: 'text', required: true, max: 120 },
    { key: 'url', label: 'URL', kind: 'text', required: true },
    { key: 'altText', label: 'Alt Text', kind: 'text', required: false, max: 255 },
  ],
  searchable: ['filename', 'altText'],
  filterable: [],
  sortable: ['filename', 'sizeBytes', 'createdAt'],
  defaultSort: { field: 'createdAt', direction: 'desc' },
  bulkActions: [],
  enrich: enrichMediaLibrary,
});


export const RESOURCES: readonly ResourceDescriptor[] = [
  productsResource,
  productVariantsResource,
  collectionsResource,
  designersResource,
  customersResource,
  couponsResource,
  giftCardsResource,
  bannersResource,
  faqsResource,
  testimonialsResource,
  suppliersResource,
  warehousesResource,
  hamperItemsResource,
  addOnsResource,
  personalisationResource,
  builderTemplatesResource,
  customerSegmentsResource,
  reviewsResource,
  recipientsResource,
  addressesResource,
  leadsResource,
  corporateAccountsResource,
  quotationsResource,
  approvalsResource,
  deliveryZonesResource,
  shippingRulesResource,
  couriersResource,
  shipmentsResource,
  exceptionsResource,
  packagingResource,
  autoDiscountsResource,
  upsellRulesResource,
  loyaltyTiersResource,
  referralsResource,
  homepageCmsResource,
  occasionPagesResource,
  seoEntriesResource,
  blogPostsResource,
  menusResource,
  mediaLibraryResource,
  integrationsResource,
  webhooksResource,
  apiKeysResource,
  auditLogsResource,
  importJobsResource,
];

const BY_SLUG = new Map(RESOURCES.map((r) => [r.slug, r]));

export const resourceBySlug = (slug: string): ResourceDescriptor | undefined => BY_SLUG.get(slug);

export const resourceSlugs = (): string[] => [...BY_SLUG.keys()];
